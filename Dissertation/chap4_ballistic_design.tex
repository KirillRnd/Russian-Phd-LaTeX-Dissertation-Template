\chapter{Управляющие параметры начального приближения в задаче баллистического проектирования}

В настоящей главе результаты, полученные для аппроксимированного модельного многообразия и \(R\)-преобразований, рассматриваются как инструмент построения начального приближения для задач баллистического проектирования. Сначала вводится система управляющих параметров. Затем формулируется алгоритм построения начального вектора расширенной системы, приводится пример его применения к перелёту Земля--Марс и оценивается качество начальных приближений для перелётов Земля--Марс и Земля--Венера. В заключительной части главы рассматривается сравнение с продолжением по гравитационному параметру, переход к постановкам с ненулевой отлётной скоростью, применение разработанной схемы при проектировании миссии Земля--Венера и её программная реализация в ILTT Toolbox.

\section{Управляющие параметры начального приближения}

В предыдущих главах были построены два класса результатов, необходимые для получения начального приближения в прикладной задаче баллистического проектирования. В главе~2 получены конечные формулы для плоской круговой модельной задачи, позволяющие восстановить продолжительность перелёта, значение функционала и начальный вектор сопряжённых переменных на аппроксимированном модельном многообразии. В главе~3 построены линейные преобразования начальных значений сопряжённых переменных, позволяющие согласованно перейти от плоской круговой постановки к пространственной задаче с учётом эллиптичности конечной орбиты. В настоящей главе эти результаты используются совместно.

Для описания начального приближения введём вектор управляющих параметров
\begin{equation}
	\mathbf q =
	\left(
	\lambda,\kappa,\zeta,\chi,j,\gamma
	\right).
\end{equation}
Эти параметры не являются новым набором граничных условий. Они задают вычислительную цепочку, с помощью которой выбирается точка на аппроксимированном модельном многообразии и затем выполняется переход к начальному вектору сопряжённых переменных для исходной пространственной задачи.

Первые два параметра, \(\lambda\) и \(\kappa\), относятся к плоской круговой модельной задаче. Параметр \(\lambda\) задаёт витковую дальность модельного перелёта, а параметр \(\kappa\) --- отношение радиусов модельных круговых орбит. Тем самым пара \((\lambda,\kappa)\) определяет точку, в которой используются конечные формулы главы~2. Для этой точки вычисляются приближённые значения продолжительности перелёта, квадратичного функционала и начального вектора сопряжённых переменных:
\begin{equation}
	(\lambda,\kappa)
	\longrightarrow
	\widehat T,\quad
	\widehat J,\quad
	\widehat{\mathbf z}^{\mathrm{pl}}_0 .
\end{equation}

Оставшиеся четыре параметра отвечают за переход от модельного решения к задаче с заданными орбитальными элементами конечной орбиты. Управляющий параметр \(\zeta\) используется в \(R\)-преобразовании II типа и связан с заданием эксцентриситета конечной орбиты. Угол \(\chi\) является управляющим угловым параметром плоского преобразования, с помощью которого задаётся аргумент перицентра \(\omega\). Параметр \(j\) используется в \(R\)-преобразовании III типа для задания наклонения конечной орбиты, а угол \(\gamma\) служит управляющим угловым параметром пространственного преобразования, связанным с контролем долготы восходящего узла.

При этом необходимо различать аргумент перицентра и долготу перицентра. В плоском \(R\)-преобразовании II типа используется аргумент перицентра \(\omega\), отсчитываемый в плоскости конечной орбиты от линии узлов. В пространственной задаче ориентация этой линии задаётся долготой восходящего узла \(\Omega\). Поэтому при переходе к полному пространственному описанию возникает долгота перицентра
\begin{equation}
	\bar{\omega}=\Omega+\omega .
\end{equation}

Такое разделение позволяет не смешивать физические орбитальные элементы конечной орбиты с параметрами вычислительной процедуры. Долгота восходящего узла \(\Omega\) и долгота перицентра \(\bar{\omega}\) характеризуют ориентацию конечной орбиты в пространстве, тогда как \(\chi\) и \(\gamma\) являются управляющими угловыми параметрами соответствующих \(R\)-преобразований.

Роли управляющих параметров сведены в табл.~\ref{tab:chap4_control_parameters}.

\begin{table}[htbp]
	\centering
	\caption{Управляющие параметры начального приближения.}
	\label{tab:chap4_control_parameters}
	\begin{tabular}{cp{0.47\textwidth}p{0.28\textwidth}}
		\toprule
		Параметр & Роль & Источник \\
		\midrule
		\(\lambda\) &
		Витковая дальность модельного перелёта; задаёт положение на аппроксимированном модельном многообразии. &
		Конечные формулы главы~2 \\
		\(\kappa\) &
		Отношение радиусов модельных круговых орбит; задаёт масштаб конечной круговой орбиты в модельной задаче. &
		Конечные формулы главы~2 \\
		\(\zeta\) &
		Управляющий параметр \(R\)-преобразования II типа, используемый для задания эксцентриситета конечной орбиты. &
		\(R\)-преобразование II типа \\
		\(\chi\) &
		Управляющий угол плоского преобразования, соответствующий заданному аргументу перицентра \(\omega\). &
		\(R\)-преобразование II типа \\
		\(j\) &
		Управляющий параметр \(R\)-преобразования III типа, используемый для задания наклонения конечной орбиты. &
		\(R\)-преобразование III типа \\
		\(\gamma\) &
		Управляющий угол пространственного преобразования, связанный с контролем долготы восходящего узла. &
		\(R\)-преобразование III типа \\
		\bottomrule
	\end{tabular}
\end{table}

Таким образом, вектор \(\mathbf q\) объединяет параметры двух уровней. Пара \((\lambda,\kappa)\) выбирает исходное приближение на плоском модельном многообразии, а параметры \((\zeta,\chi,j,\gamma)\) задают последовательность линейных преобразований начальных значений сопряжённых переменных. При этом аргумент перицентра \(\omega\), используемый в плоском преобразовании, согласуется с пространственной ориентацией орбиты через долготу восходящего узла \(\Omega\), что позволяет перейти к долготе перицентра \(\bar{\omega}\). В результате конечные формулы и \(R\)-преобразования образуют единую схему построения начального приближения без предварительного решения двухточечной краевой задачи.

\section{Алгоритм построения начального приближения}

Алгоритм построения начального приближения использует результаты глав~2 и~3 не как независимые эмпирические блоки, а как единую вычислительную схему. Сначала реальная эфемеридная постановка приводится к безразмерной форме, затем по начальной синодической фазе выбирается одна из точек оптимальной дальности на КЛО, после чего к соответствующему модельному решению применяются линейные преобразования начальных значений сопряжённых переменных.

Пусть задана дата старта и, следовательно, из эфемерид известен начальный фазовый вектор тела отправления
\begin{equation}
	\mathbf x_{0,d}=(\mathbf r_{0,d},\mathbf v_{0,d})
\end{equation}
и целевые орбитальные элементы конечной орбиты относительно центрального тела. Переход к безразмерной постановке выполняется так же, как в главе~1: выбирается единица расстояния \(DU\), единица времени \(TU\) определяется из условия
\begin{equation}
	TU=\sqrt{\frac{DU^3}{\mu_d}},
\end{equation}
и после нормировки гравитационный параметр центрального тела становится равным единице. В алгоритме используются безразмерные векторы
\begin{equation}
	\mathbf r_0=\frac{\mathbf r_{0,d}}{DU},
	\qquad
	\mathbf v_0=\frac{TU}{DU}\mathbf v_{0,d}.
\end{equation}
Таким образом, орбиты и дата старта берутся из эфемерид, но вычислительная схема работает в той же нормированной задаче двух тел, что и модельное многообразие.

Из целевой конечной орбиты в той же безразмерной системе определяются величины, необходимые для выбора управляющих параметров:
\begin{equation}
	\left(a,e,\omega,i,\Omega \right)\longrightarrow \left( \kappa,
	\zeta,\chi,j,\gamma
	\right).
\end{equation}
Здесь \(a\) задаёт полуось конечной орбиты, \(e\) --- эксцентриситет, \(i\) --- наклонение, \(\Omega\) --- долгота восходящего узла, а \(\omega\) --- аргумент перицентра. Выбор витковой дальности \(\lambda\) не является произвольным. Он выполняется через точки оптимальной дальности, введённые в главе~2. Для заданной даты старта сначала определяется начальная синодическая фаза \(\Phi\).

\begin{equation}
	\left( t_0 \right)\longrightarrow \left( \Phi	\right).
\end{equation}
Затем рассматриваются пересечения изолинии этой фазы с КЛО, то есть решается обратная задача выбора локально оптимальной витковой дальности. Так как фаза определена с точностью до целого числа оборотов, это приводит к счётному набору возможных точек:
\begin{equation}
	\lambda_k^*=\lambda^*(\Phi,k,\kappa),
	\qquad
	k\in\mathbb Z.
\end{equation}
В качестве кандидатов рассматриваются три соседние ветви
\begin{equation}
	k\in\{-1,0,1\},
\end{equation}
поскольку именно они соответствуют ближайшим точкам оптимальной дальности, лежащим в рабочей области продолжительности перелёта. Для каждого допустимого кандидата вычисляются конечные формулы главы~2, а затем выбирается вариант, удовлетворяющий ограничениям задачи и дающий наилучшее значение выбранного критерия. Если только одна из трёх точек попадает в допустимую область, выбор становится однозначным.

После выбора точки оптимальной дальности определяется пара
\begin{equation}
	(\lambda^*,\kappa),
\end{equation}
которая задаёт точку аппроксимированного модельного многообразия. Затем учитывается побочная деформация витковой дальности, возникающая при \(R\)-преобразовании II типа: вместо исходного значения используется скорректированная величина
\begin{equation}
	\lambda_{\mathrm{corr}}=\lambda^*-\Delta\lambda.
\end{equation}
Тем самым конечные формулы главы~2 используются в точке \((\lambda_{\mathrm{corr}},\kappa)\), выбранной по КЛО и согласованной с последующим плоским преобразованием.

По конечным формулам главы~2 вычисляются приближённые характеристики модельного решения:
\begin{equation}
	(\lambda_{\mathrm{corr}},\kappa)
	\longrightarrow
	\widehat T,\quad
	\widehat J,\quad
	\widehat{\mathbf z}^{\mathrm{pl}}_0 .
\end{equation}
Здесь
\begin{equation}
	\widehat{\mathbf z}^{\mathrm{pl}}_0=
	\left(
	\widehat{\mathbf p}_{r0}^{\mathrm{pl}},
	\widehat{\mathbf p}_{v0}^{\mathrm{pl}}
	\right)
\end{equation}
является начальным вектором сопряжённых переменных для плоской круговой модельной задачи. На этом этапе ещё не учитываются ни заданный начальный фазовый вектор из эфемерид, ни эллиптичность и пространственная ориентация конечной орбиты.

Связь между элементами конечной орбиты и линейными преобразованиями задаётся следующим образом. \(R\)-преобразование I типа используется для перехода от модельного начального фазового вектора к заданному начальному фазовому вектору:
\begin{equation}
	R_0=R(\mathbf r_0,\mathbf v_0).
\end{equation}
Это преобразование отвечает за согласование модельной круговой постановки с реальным положением и скоростью тела отправления, полученными из эфемерид.

\(R\)-преобразование II типа используется для задания формы конечной орбиты в плоскости. Его управляющий параметр \(\zeta\) определяется из условия задания эксцентриситета \(e\), а управляющий угол \(\chi\) --- из условия задания аргумента перицентра \(\omega\):
\begin{equation}
	(e,\omega)
	\longrightarrow
	(\zeta,\chi)
	\longrightarrow
	R_{e\omega}.
\end{equation}
При \(e=0\) аргумент перицентра вырождается; в этом случае плоская деформация, связанная с эксцентриситетом, отсутствует и \(\zeta\) полагается равным нулю.

\(R\)-преобразование III типа используется для задания пространственной ориентации конечной орбиты. Его управляющий параметр \(j\) определяется из условия задания наклонения \(i\), а управляющий угол \(\gamma\) связан с контролем долготы восходящего узла:
\begin{equation}
	(i,\Omega)
	\longrightarrow
	(j,\gamma)
	\longrightarrow
	R_{i\Omega}.
\end{equation}
При \(i=0\) пространственное преобразование также становится вырожденным, и параметр \(j\) полагается равным нулю.

Итоговое преобразование начального вектора сопряжённых переменных записывается как композиция
\begin{equation}
	R_{\Sigma}
	=
	R_{i\Omega}
	R_{e\omega}
	R_0 .
\end{equation}
Порядок сомножителей отражает порядок учёта геометрии задачи: сначала задаётся начальный фазовый вектор, затем форма конечной орбиты в плоскости, после чего учитывается пространственная ориентация орбиты. В соответствии с правилом преобразования сопряжённых переменных получаем
\begin{equation}
	\mathbf p_{r0}^{\mathrm{init}}
	=
	(R_{\Sigma}^{T})^{-1}
	\widehat{\mathbf p}_{r0}^{\mathrm{pl}},
	\qquad
	\mathbf p_{v0}^{\mathrm{init}}
	=
	(R_{\Sigma}^{T})^{-1}
	\widehat{\mathbf p}_{v0}^{\mathrm{pl}}.
\end{equation}
Тем самым начальный вектор расширенной системы принимает вид
\begin{equation}
	\mathbf y_0^{\mathrm{init}}
	=
	\left(
	\mathbf r_0,
	\mathbf v_0,
	\mathbf p_{r0}^{\mathrm{init}},
	\mathbf p_{v0}^{\mathrm{init}}
	\right).
\end{equation}
Вместе с приближённой продолжительностью перелёта \(\widehat T\) он задаёт начальное приближение для последующего решения двухточечной краевой задачи.

Основные шаги алгоритма сведены в табл.~\ref{tab:chap4_initial_guess_algorithm}.

\begin{table}[htbp]
	\centering
	\caption{Алгоритм построения начального приближения.}
	\label{tab:chap4_initial_guess_algorithm}
	\begin{tabular}{cp{0.56\textwidth}p{0.26\textwidth}}
		\toprule
		Шаг & Действие & Опора \\
		\midrule
		1 &
		Получить из эфемерид начальный фазовый вектор и целевые элементы конечной орбиты. &
		Эфемеридная постановка \\
		2 &
		Перейти к безразмерным переменным при \(\mu=1\). &
		Нормировка главы~1 \\
		3 &
		Определить \(\kappa,e,i,\Omega,\bar{\omega}\) и вычислить \(\omega=\bar{\omega}-\Omega\). &
		Геометрия конечной орбиты \\
		4 &
		По начальной синодической фазе выбрать кандидаты \(\lambda_k^*\) на КЛО для \(k=-1,0,1\). &
		Точки оптимальной дальности главы~2 \\
		5 &
		Выбрать допустимую точку оптимальной дальности и получить пару \((\lambda^*,\kappa)\). &
		КЛО и конечные формулы главы~2 \\
		6 &
		Вычислить поправку к \(\lambda^*\) для компенсации деформации. &
		Коррекция витковой дальности главы~3 \\
		7 &
		Вычислить \(\widehat T\), \(\widehat J\) и \(\widehat{\mathbf z}^{\mathrm{pl}}_0\) в точке \((\lambda_{\mathrm{corr}},\kappa)\). &
		Аппроксимированное многообразие \\
		8 &
		Вычислить \((\zeta,\chi)\) по \(e,\omega\) и \((j,\gamma)\) по \(i,\Omega\). &
		\(R\)-преобразования главы~3 \\
		9 &
		Построить композицию \(R_{\Sigma}=R_{i\Omega}R_{e\omega}R_0\). &
		Композиция \(R\)-преобразований \\
		10 &
		Преобразовать начальный вектор сопряжённых переменных и получить \(\mathbf y_0^{\mathrm{init}}\). &
		Расширенная система \\
		\bottomrule
	\end{tabular}
\end{table}

С вычислительной точки зрения алгоритм имеет постоянную сложность относительно числа возможных дат старта и не требует решения краевой задачи на этапе построения начального приближения. Все величины вычисляются прямой подстановкой в конечные формулы и матрицы \(R\)-преобразований. Единственный дискретный выбор связан с проверкой не более трёх точек оптимальной дальности, соответствующих индексам \(k=-1,0,1\). Поэтому трудоёмкость построения одного начального приближения можно рассматривать как \(O(1)\); если допустимая точка оптимальной дальности заранее задана, алгоритм сводится к набору прямых вычислений без перебора.

Полученный вектор \(\mathbf y_0^{\mathrm{init}}\) не является точным решением краевой задачи. Однако он уже согласован с геометрией модельного многообразия, выбранной точкой КЛО, начальным фазовым вектором из эфемерид и основными элементами конечной орбиты. Поэтому последующее численное уточнение начинается не с произвольного начального вектора сопряжённых переменных, а с вектора, построенного по конечным формулам и линейным преобразованиям.

\section{Пример построения начального приближения для перелёта Земля--Марс}

Рассмотрим применение описанного алгоритма на примере перелёта Земля--Марс. В этом примере результаты глав~2 и~3 собираются в одно начальное приближение для конкретной эфемеридной задачи. Использовались эфемериды JPL DE430~\cite{folkner_de430_2014}.
В качестве даты старта выбрана юлианская дата
\[
t_0=2461193.0.
\]
По эфемеридам определяются положение и скорость Земли в начальный момент времени, а также целевые элементы орбиты Марса. Затем выполняется переход к безразмерной постановке. В данном примере
\[
DU=1~\text{а.е.}=1.495979\times10^{8}~\text{км},
\qquad
TU=5.022643\times10^{6}~\text{с}=5.813244\times10^{1}~\text{сут},
\]
и после нормировки гравитационный параметр центрального тела равен единице.

Начальный фазовый вектор в безразмерных переменных имеет вид
\[
\mathbf r_0=
\begin{pmatrix}
	-3.030750\times10^{-1}\\
	-9.683082\times10^{-1}\\
	\phantom{-}2.218031\times10^{-5}
\end{pmatrix},
\qquad
\mathbf v_0=
\begin{pmatrix}
	\phantom{-}9.380035\times10^{-1}\\
	-3.021758\times10^{-1}\\
	\phantom{-}3.360727\times10^{-5}
\end{pmatrix}.
\]

Для выбранной даты старта начальная синодическая фаза составляет
\[
\Psi_0=3.481030\times10^{-1}
\]
в оборотах. По этой фазе выбирается точка оптимальной дальности на КЛО. В данном примере выбран индекс
\[
k=0.
\]
До учёта коррекции витковой дальности получено
\[
\lambda^*=1.324331\times10^{0}.
\]
После компенсации деформации, возникающей при \(R\)-преобразовании II типа, используется значение
\[
\lambda_{\mathrm{corr}}=1.268512\times10^{0}.
\]
Отношение радиусов модельных круговых орбит равно
\[
\kappa=1.523783\times10^{0}.
\]
Таким образом, конечные формулы главы~2 применяются в точке \((\lambda_{\mathrm{corr}},\kappa)\).

Целевые элементы конечной орбиты Марса в рассматриваемый момент времени имеют вид
\[
a=1.523783\times10^{0},\qquad
e=9.336536\times10^{-2},\qquad
i=3.226036\times10^{-2}.
\]
Для ориентации орбиты получены значения
\[
\Omega=8.640848\times10^{-1},
\qquad
\bar{\omega}=5.865724\times10^{0}.
\]
В используемой реализации углы \(R\)-преобразований задаются относительно начальной истинной долготы
\[
L_0=4.409054\times10^{0}.
\]
Поэтому для плоского преобразования используется относительный угол
\[
\bar{\omega}_{\mathrm{rel}}=\bar{\omega}-L_0=1.456670\times10^{0},
\]
а для пространственного преобразования
\[
\Omega_{\mathrm{rel}}=\Omega-L_0=-3.544969\times10^{0},
\qquad
\gamma=\Omega_{\mathrm{rel}}.
\]
Эквивалентное приведённое к основному диапазону значение управляющего угла составляет
\[
\gamma=2.738216\times10^{0}.
\]

По конечным формулам главы~3 вычисляются управляющие параметры
\[
\begin{aligned}
	\zeta &= -9.950813\times10^{-2}, &
	\chi &= \phantom{-}7.522721\times10^{-1},\\
	j &= -2.291710\times10^{-1}, &
	\gamma &= -3.544969\times10^{0}.
\end{aligned}
\]
Тем самым для рассматриваемого примера начальный вектор управляющих параметров принимает вид
\begin{equation}
	\mathbf q_{\mathrm{init}}=
	\begin{pmatrix}
		\phantom{-}1.268512\times10^{0\phantom{-}}\\
		\phantom{-}1.523783\times10^{0\phantom{-}}\\
		-9.950813\times10^{-2}\\
		\phantom{-}7.522721\times10^{-1}\\
		-2.291710\times10^{-1}\\
		-3.544969\times10^{0\phantom{-}}
	\end{pmatrix}.
\end{equation}

По конечным формулам главы~2 получаем модельный начальный вектор сопряжённых переменных
\[
\widehat{\mathbf p}_{r0}^{\mathrm{pl}}=
\begin{pmatrix}
	\phantom{-}1.982747\times10^{-2}\\
	\phantom{-}1.198093\times10^{-3}\\
	\phantom{-}0
\end{pmatrix},
\qquad
\widehat{\mathbf p}_{v0}^{\mathrm{pl}}=
\begin{pmatrix}
	\phantom{-}1.353337\times10^{-3}\\
	\phantom{-}1.790549\times10^{-2}\\
	\phantom{-}0
\end{pmatrix}.
\]
После применения композиции \(R\)-преобразований получаем начальный вектор сопряжённых переменных для эфемеридной задачи:
\[
\mathbf p_{r0}^{\mathrm{init}}=
\begin{pmatrix}
	-7.012121\times10^{-3}\\
	-1.785764\times10^{-2}\\
	-1.984265\times10^{-3}
\end{pmatrix},
\qquad
\mathbf p_{v0}^{\mathrm{init}}=
\begin{pmatrix}
	\phantom{-}1.747309\times10^{-2}\\
	-4.626026\times10^{-3}\\
	-3.796719\times10^{-3}
\end{pmatrix}.
\]

Основные скалярные величины для рассматриваемого примера приведены в табл.~\ref{tab:chap4_mars_example_parameters}. Таблица разделяет целевые элементы конечной орбиты, управляющие параметры алгоритма и характеристики полученного начального приближения.

\begin{table}[htbp]
	\centering
	\caption{Целевые элементы конечной орбиты и управляющие параметры начального приближения для перелёта Земля--Марс.}
	\label{tab:chap4_mars_example_parameters}
	\begin{tabular}{cc}
		\toprule
		Величина & Значение \\
		\midrule
		\multicolumn{2}{c}{Целевые элементы конечной орбиты} \\
		\midrule
		\(a\) & \(\phantom{-}1.523783\times10^{0\phantom{-}}\) \\
		\(e\) & \(\phantom{-}9.336536\times10^{-2}\) \\
		\(i\), рад & \(\phantom{-}3.226036\times10^{-2}\) \\
		\(\Omega\), рад & \(\phantom{-}8.640848\times10^{-1}\) \\
		\(\bar{\omega}\), рад & \(\phantom{-}5.865724\times10^{0\phantom{-}}\) \\
		\(L_0\), рад & \(\phantom{-}4.409054\times10^{0\phantom{-}}\) \\
		\(\bar{\omega}_{\mathrm{rel}}\), рад & \(\phantom{-}1.456670\times10^{0\phantom{-}}\) \\
		\(\Omega_{\mathrm{rel}}\), рад & \(-3.544969\times10^{0\phantom{-}}\) \\
		\midrule
		\multicolumn{2}{c}{Управляющие параметры} \\
		\midrule
		\(\lambda_{\mathrm{corr}}\) & \(\phantom{-}1.268512\times10^{0\phantom{-}}\) \\
		\(\kappa\) & \(\phantom{-}1.523783\times10^{0\phantom{-}}\) \\
		\(\zeta\) & \(-9.950813\times10^{-2}\) \\
		\(\chi\), рад & \(\phantom{-}7.522721\times10^{-1}\) \\
		\(j\) & \(-2.291710\times10^{-1}\) \\
		\(\gamma\), рад & \(-3.544969\times10^{0\phantom{-}}\) \\
		\midrule
		\multicolumn{2}{c}{Характеристики начального приближения} \\
		\midrule
		\(\widehat T\), сут & \(\phantom{-}6.361619\times10^{2\phantom{-}}\) \\
		\(\widehat J\) & \(\phantom{-}1.324138\times10^{-3}\) \\
		\(m_p\), кг & \(\phantom{-}4.543364\times10^{1\phantom{-}}\) \\
		\(\|\Delta \mathbf r_f\|\), км & \(\phantom{-}6.538725\times10^{7\phantom{-}}\) \\
		\(\|\Delta \mathbf v_f\|\), км/с & \(\phantom{-}8.880610\times10^{0\phantom{-}}\) \\
		\(T_{\mathrm{opt}}\), сут & \(\phantom{-}6.538369\times10^{2\phantom{-}}\) \\
		\(J_{\mathrm{opt}}\) & \(\phantom{-}1.796705\times10^{-3}\) \\
		\(m_{p,\mathrm{opt}}\), кг & \(\phantom{-}5.903983\times10^{1\phantom{-}}\) \\
		\(\|\Delta \mathbf r_f\|_{\mathrm{opt}}\), км & \(\phantom{-}8.074021\times10^{-4}\) \\
		\(\|\Delta \mathbf v_f\|_{\mathrm{opt}}\), км/с & \(\phantom{-}9.409393\times10^{-11}\) \\
		\bottomrule
	\end{tabular}
\end{table}

В табл.~\ref{tab:chap4_mars_example_vectors} приведены векторные величины: начальный фазовый вектор, модельный начальный вектор сопряжённых переменных, начальный вектор сопряжённых переменных после \(R\)-преобразований и уточнённый вектор после решения краевой задачи.

\begin{table}[htbp]
	\centering
	\caption{Начальный вектор расширенной системы для примера перелёта Земля--Марс.}
	\label{tab:chap4_mars_example_vectors}
	\begin{tabular}{cp{0.9\textwidth}}
		\toprule
		Величина & Значение \\
		\midrule
		\(\mathbf r_0\) &
		\((-3.030750\times10^{-1},\, -9.683082\times10^{-1},\, \phantom{-}2.218031\times10^{-5})^T\) \\
		\(\mathbf v_0\) &
		\((\phantom{-}9.380035\times10^{-1},\, -3.021758\times10^{-1},\, \phantom{-}3.360727\times10^{-5})^T\) \\
		\(\widehat{\mathbf p}_{r0}^{\mathrm{pl}}\) &
		\((\phantom{-}1.982747\times10^{-2},\, \phantom{-}1.198093\times10^{-3},\, \phantom{-}0)^T\) \\
		\(\widehat{\mathbf p}_{v0}^{\mathrm{pl}}\) &
		\((\phantom{-}1.353337\times10^{-3},\, \phantom{-}1.790549\times10^{-2},\, \phantom{-}0)^T\) \\
		\(\mathbf p_{r0}^{\mathrm{init}}\) &
		\((-7.012121\times10^{-3},\, -1.785764\times10^{-2},\, -1.984265\times10^{-3})^T\) \\
		\(\mathbf p_{v0}^{\mathrm{init}}\) &
		\((\phantom{-}1.747309\times10^{-2},\, -4.626026\times10^{-3},\, -3.796719\times10^{-3})^T\) \\
		\(\mathbf p_{r0}^{\mathrm{opt}}\) &
		\((-7.840130\times10^{-3},\, -2.028390\times10^{-2},\, -1.991360\times10^{-3})^T\) \\
		\(\mathbf p_{v0}^{\mathrm{opt}}\) &
		\((\phantom{-}2.053753\times10^{-2},\, -5.524200\times10^{-3},\, -4.491670\times10^{-3})^T\) \\
		\bottomrule
	\end{tabular}
\end{table}

Начальный вектор расширенной системы, используемый далее как приближение для численного уточнения, имеет вид
\begin{equation}
	\mathbf y_0^{\mathrm{init}}
	=
	\left(
	\mathbf r_0,
	\mathbf v_0,
	\mathbf p_{r0}^{\mathrm{init}},
	\mathbf p_{v0}^{\mathrm{init}}
	\right).
\end{equation}
После решения краевой задачи начальные значения сопряжённых переменных переходят к значениям \(\mathbf p_{r0}^{\mathrm{opt}}\) и \(\mathbf p_{v0}^{\mathrm{opt}}\), приведённым в табл.~\ref{tab:chap4_mars_example_vectors}.

На рис.~\ref{fig:chap4_mars_example} показано сравнение траектории, полученной из построенного начального приближения, и траектории после численного уточнения краевой задачи. Видно, что начальное приближение воспроизводит общий характер многовиткового перелёта и оказывается достаточно близким к уточнённой траектории для последующего применения метода пристрелки.

\begin{figure}[htbp]
	\centering
	\includegraphics[width=0.82\textwidth]{images/chap4_mars_cleanver_rebuilt_Oxy.png}
	\caption{Сравнение начального приближения и уточнённой траектории для перелёта Земля--Марс.}
	\label{fig:chap4_mars_example}
\end{figure}

Таким образом, в рассматриваемом примере начальное приближение строится без предварительного решения двухточечной краевой задачи. Точка КЛО задаёт модельную основу перелёта, конечные формулы главы~2 дают начальный вектор сопряжённых переменных, а \(R\)-преобразования главы~3 согласуют это приближение с эфемеридным начальным фазовым вектором и элементами конечной орбиты. Последующее численное уточнение уменьшает невязки правого конца с \(6.538725\times10^{7}\) км и \(8.880610\times10^{0}\) км/с для начального приближения до \(8.074021\times10^{-4}\) км и \(9.409393\times10^{-11}\) км/с после уточнения.

\section{Оценка качества начальных приближений для перелётов Земля--Марс и Земля--Венера}

В предыдущем разделе был рассмотрен один пример построения начального приближения для перелёта Земля--Марс. Для оценки устойчивости алгоритма используется представительная последовательность эфемеридных конфигураций. В настоящем разделе такая проверка выполняется для перелётов Земля--Марс и Земля--Венера.

Для выбора диапазона дат старта используется понятие периода великих противостояний. В случае Земли и Марса этим термином в астрономии называют промежуток времени между двумя последовательными сближениями планет до минимального возможного расстояния. При этом обе планеты располагаются по одну сторону от Солнца: Земля находится вблизи афелия, а Марс --- вблизи перигелия. Такая конфигурация повторяется через \(17\) лет. Следуя этому определению, для пары Земля--планета будем использовать период великих противостояний как наименьший промежуток времени, через который повторяется конфигурация двух планет в гелиоцентрической системе координат~\cite{sikharulidze_ballistika_2020}.

Значения периодов, используемые в настоящем разделе, приведены в табл.~\ref{tab:chap4_synodic_ca_periods}. Здесь \(T_{\mathrm{syn}}\) --- синодический период планеты относительно Земли, \(T_{\mathrm{sid}}\) --- сидерический период целевой планеты, а \(T_{\mathrm{ca}}\) --- период великих противостояний.

\begin{table}[htbp]
	\centering
	\caption{Синодические, сидерические периоды и периоды великих противостояний для перелётов Земля--Венера и Земля--Марс.}
	\label{tab:chap4_synodic_ca_periods}
	\begin{tabular}{cccc}
		\toprule
		Целевая планета &
		\(T_{\mathrm{syn}}\), лет &
		\(T_{\mathrm{sid}}\), лет &
		\(T_{\mathrm{ca}}\), лет \\
		\midrule
		Венера & \(1.599\) & \(0.615\) & \(8\) \\
		Марс  & \(2.135\) & \(1.881\) & \(17\) \\
		\bottomrule
	\end{tabular}
\end{table}

В численном эксперименте ошибки начального приближения рассчитывались для каждой даты старта на периодах великих противостояний. Такой выбор задаёт один полный цикл повторения взаимной конфигурации планет в гелиоцентрической системе координат и позволяет оценить качество алгоритма не только для отдельного примера, но и для широкого набора геометрий перелёта.

В качестве меры ошибки используется средняя абсолютная ошибка
\[
\mathrm{MAE}
=
\frac{1}{N_{\mathrm{samp}}}
\sum_{r=1}^{N_{\mathrm{samp}}}
\left|y_r^{\mathrm{init}}-y_r^{\mathrm{target}}\right|,
\]
где \(y_r^{\mathrm{init}}\) --- значение характеристики конечной орбиты или траектории, полученное после интегрирования начального приближения, а \(y_r^{\mathrm{target}}\) --- соответствующее целевое значение. Для угловых величин ошибка вычислялась с учётом периодичности угла.

Результаты расчётов приведены в табл.~\ref{tab:chap4_initial_guess_errors}. В таблице отдельно представлены ошибки для параметров конечной орбиты, угловой дальности, функционала и продолжительности перелёта. Эти величины характеризуют качество начального приближения до последующего численного уточнения краевой задачи.

\begin{table}[htbp]
	\centering
	\caption{Средняя абсолютная ошибка начального приближения для перелётов Земля--Марс и Земля--Венера.}
	\label{tab:chap4_initial_guess_errors}
	\begin{tabular}{ccc}
		\toprule
		Целевой параметр &
		Перелёт Земля--Марс &
		Перелёт Земля--Венера \\
		\midrule
		Эксцентриситет \(e\) &
		\(\phantom{-}3.00\times10^{-3}\) &
		\(\phantom{-}2.00\times10^{-2}\) \\
		Наклонение \(i\) &
		\(\phantom{-}4.00\times10^{-3}\,{}^\circ\) &
		\(\phantom{-}2.00\times10^{-1}\,{}^\circ\) \\
		ДВУ \(\Omega\) &
		\(\phantom{-}4.00\times10^{0\phantom{-}}\,{}^\circ\) &
		\(\phantom{-}2.00\times10^{0\phantom{-}}\,{}^\circ\) \\
		Аргумент перицентра \(\omega\) &
		\(\phantom{-}1.10\times10^{1\phantom{-}}\,{}^\circ\) &
		\(\phantom{-}2.60\times10^{1\phantom{-}}\,{}^\circ\) \\
		Угловая дальность \(\varphi\) &
		\(\phantom{-}3.00\times10^{0\phantom{-}}\,{}^\circ\) &
		\(\phantom{-}7.00\times10^{-1}\,{}^\circ\) \\
		Большая полуось \(a\) &
		\(\phantom{-}6.00\times10^{-2}\) а.е. &
		\(\phantom{-}4.00\times10^{-3}\) а.е. \\
		Функционал \(J\) &
		\(\phantom{-}8.00\times10^{-2}\) м\(^2\)/с\(^3\) &
		\(\phantom{-}3.00\times10^{-3}\) м\(^2\)/с\(^3\) \\
		Продолжительность перелёта \(T\) &
		\(\phantom{-}8.50\times10^{0\phantom{-}}\) сут. &
		\(\phantom{-}7.00\times10^{-1}\) сут. \\
		\bottomrule
	\end{tabular}
\end{table}

Из табл.~\ref{tab:chap4_initial_guess_errors} видно, что построенное начальное приближение сохраняет приемлемую точность на всём выбранном диапазоне дат старта. Для перелёта Земля--Марс наибольшие ошибки возникают в ориентации линии апсид и в большой полуоси конечной орбиты. Это связано с более сложной геометрией перелёта к внешней планете и с большей чувствительностью многовитковой траектории к выбору точки оптимальной дальности. При этом ошибка продолжительности перелёта остаётся порядка нескольких суток на периоде в \(17\) лет.

Для перелёта Земля--Венера ошибки по большой полуоси, угловой дальности, функционалу и продолжительности перелёта заметно меньше. Вместе с тем ошибка аргумента перицентра оказывается больше, чем в марсианском случае. Это указывает на то, что различные элементы конечной орбиты имеют разную чувствительность к выбранным управляющим параметрам, а качество начального приближения следует оценивать не по одной величине, а по совокупности характеристик.

Полученные ошибки не означают, что конечные формулы должны непосредственно заменять решение краевой задачи. Их основная роль состоит в построении начального вектора расширенной системы, близкого к требуемому решению. При этом управляющие параметры
\[
\mathbf q=(\lambda,\kappa,\zeta,\chi,j,\gamma)
\]
могут использоваться не только для однократного построения начального приближения, но и как переменные последующего численного уточнения. В этом случае вместо прямой оптимизации по шести начальным значениям сопряжённых переменных задача решается в параметрах, имеющих ясный баллистический смысл: витковая дальность, отношение радиусов модельных орбит и параметры линейных преобразований.

Для проверки такого подхода была выполнена дополнительная оптимизация в параметрах \(\mathbf q\). Начальное значение \(\mathbf q_{\mathrm{init}}\) выбиралось по описанному выше алгоритму, после чего параметры уточнялись так, чтобы уменьшить невязки целевых элементов конечной орбиты и угловой дальности. Полученные невязки приведены в табл.~\ref{tab:chap4_q_optimization_residuals}.

\begin{table}[htbp]
	\centering
	\caption{Невязки целевых параметров после оптимизации в параметрах начального приближения.}
	\label{tab:chap4_q_optimization_residuals}
	\begin{tabular}{ccc}
		\toprule
		Целевой параметр &
		Перелёт Земля--Марс &
		Перелёт Земля--Венера \\
		\midrule
		Эксцентриситет \(e\) &
		\(\phantom{-}3.90\times10^{-15}\) &
		\(\phantom{-}1.90\times10^{-12}\) \\
		Наклонение \(i\) &
		\(\phantom{-}2.10\times10^{-14}\,{}^\circ\) &
		\(\phantom{-}2.90\times10^{-13}\,{}^\circ\) \\
		ДВУ \(\Omega\) &
		\(\phantom{-}5.70\times10^{-14}\,{}^\circ\) &
		\(\phantom{-}4.70\times10^{-13}\,{}^\circ\) \\
		Аргумент перицентра \(\omega\) &
		\(\phantom{-}3.00\times10^{-13}\,{}^\circ\) &
		\(\phantom{-}1.20\times10^{-13}\,{}^\circ\) \\
		Угловая дальность \(\varphi\) &
		\(\phantom{-}1.70\times10^{-13}\,{}^\circ\) &
		\(\phantom{-}3.30\times10^{-16}\,{}^\circ\) \\
		Большая полуось \(a\) &
		\(\phantom{-}1.00\times10^{-14}\) а.е. &
		\(\phantom{-}6.30\times10^{-13}\) а.е. \\
		\bottomrule
	\end{tabular}
\end{table}

Табл.~\ref{tab:chap4_q_optimization_residuals} показывает, что после уточнения в параметрах начального приближения целевые параметры конечной орбиты восстанавливаются практически до уровня численной точности. Этот результат имеет два следствия. Во-первых, он показывает, что управляющие параметры \(\mathbf q\) задают не только грубую эвристику, но и пригодную локальную параметризацию окрестности решения. Во-вторых, он позволяет использовать конечные формулы как первый этап автоматического построения решения: формулы дают начальное значение \(\mathbf q_{\mathrm{init}}\), а последующее уточнение выполняется уже в маломерном пространстве физически интерпретируемых параметров.

Таким образом, численная проверка на периодах великих противостояний показывает, что алгоритм построения начального приближения устойчиво работает для двух характерных межпланетных перелётов: к внешней планете и к внутренней планете. Ошибки табл.~\ref{tab:chap4_initial_guess_errors} характеризуют качество непосредственного применения конечных формул и \(R\)-преобразований, а невязки табл.~\ref{tab:chap4_q_optimization_residuals} показывают, что та же параметризация может быть использована как основа для дальнейшего численного уточнения. Поэтому предложенный алгоритм не заменяет решение двухточечной краевой задачи, но задаёт начальный вектор расширенной системы и набор управляющих параметров, уже согласованных с основной геометрией эфемеридной задачи.

\section{Сравнение с продолжением по гравитационному параметру}
\label{sec:ch4_gravity_continuation_comparison}

В предыдущих разделах был описан алгоритм построения начального приближения с использованием управляющих параметров. После построения начального приближения эти же параметры используются как переменные уточнения. Тем самым решение краевой задачи сводится к решению системы невязок по вектору управляющих параметров \(\mathbf q\).

В расчётах уточнение выполнялось методом Левенберга--Марквардта~\cite{levenberg_method_1944,marquardt_algorithm_1963}. В используемой реализации решалась задача
\begin{equation}
	\mathbf F(\mathbf q)=0
\end{equation}
где \(\mathbf F\) --- вектор невязок правого конца, включающий невязки по элементам конечной орбиты и витковой дальности. 

Этот подход сравнивается с методом продолжения по гравитационному параметру. Такой метод относится к классическим способам построения решений краевых задач для перелётов с малой тягой и близок по идее к методам продолжения, используемым в работах В.Г.~Петухова~\cite{petukhov_method_2012}. Сначала напомним общий вид метода продолжения по параметру. Пусть
\begin{equation}
	\mathbf F(\mathbf z_0)=0
\end{equation}
--- система нелинейных уравнений двухточечной краевой задачи, где \(\mathbf z_0\) --- начальный вектор сопряжённых переменных, вместе с граничными условиями однозначно задающий траекторию. Пусть \(\mathbf z_0(0)\) --- начальное приближение и
\begin{equation}
	\mathbf F(\mathbf z_0(0))=\mathbf b.
\end{equation}
Вводится однопараметрическое семейство \(\mathbf z_0(\tau)\), \(\tau\in[0,1]\), такое что
\begin{equation}
	\mathbf F(\mathbf z_0(\tau))=(1-\tau)\mathbf b.
\end{equation}
Тогда при \(\tau=1\) получаем
\begin{equation}
	\mathbf F(\mathbf z_0(1))=0,
\end{equation}
то есть \(\mathbf z_0(1)\) является начальным вектором сопряжённых переменных для решения исходной краевой задачи. Дифференцируя условие продолжения по \(\tau\), получаем задачу Коши
\begin{equation}
	\frac{d\mathbf z_0}{d\tau}
	=
	-
	\left(
	\frac{\partial \mathbf F}{\partial \mathbf z_0}
	\right)^{-1}
	\mathbf b,
	\qquad
	\mathbf z_0(0)=\mathbf z_0^{\mathrm{init}}.
\end{equation}
Таким образом, исходная система нелинейных уравнений заменяется интегрированием задачи Коши по параметру продолжения.

В модификации с фиксированной угловой дальностью искусственно изменяется гравитационный параметр центрального тела. Вводится функция
\begin{equation}
	\bar\mu(\tau)=\mu_0+(\mu-\mu_0)\tau,
	\qquad
	\tau\in[0,1],
\end{equation}
где \(\mu\) --- реальное значение гравитационного параметра, а \(\mu_0\) выбирается так, чтобы пассивное движение по начальной орбите соответствовало заданному числу полных оборотов \(N_{\mathrm{br}}\). Пусть \(\theta_0\) --- истинная аномалия точки старта на начальной орбите, \(\varphi\) --- угол между \(\mathbf r_0\) и проекцией \(\mathbf r_f\) на плоскость начальной орбиты, \(a_0\) --- большая полуось начальной орбиты. Тогда
\begin{equation}
	\theta_f=\theta_0+\varphi.
\end{equation}
Средние аномалии \(M_0\) и \(M_f\) вычисляются через эксцентрическую аномалию \(E\):
\begin{equation}
	M=E-e\sin E,
\end{equation}
\begin{equation}
	\tan\frac{E}{2}
	=
	\sqrt{\frac{1-e}{1+e}}
	\tan\frac{\theta}{2}.
\end{equation}
Значение \(\mu_0\), соответствующее \(N_{\mathrm{br}}\) полным оборотам за время перелёта \(t_f-t_0\), задаётся формулой
\begin{equation}
	\mu_0
	=
	a_0^3
	\left(
	\frac{M_f+2\pi N_{\mathrm{br}}-M_0}{t_f-t_0}
	\right)^2.
\end{equation}
Тем самым номер \(N_{\mathrm{br}}\) задаёт ветвь продолжения.

Для задачи встречи вектор невязок при продолжении по гравитационному параметру имеет вид
\begin{equation}
	\mathbf F(\mathbf z_0,\tau)
	=
	\begin{pmatrix}
		\mathbf r(t_f)-\mathbf r_f\\
		\mathbf v(t_f)-\mathbf v_f\sqrt{\dfrac{\bar\mu(\tau)}{\mu}}
	\end{pmatrix}.
\end{equation}
Здесь масштабирование скорости используется потому, что при изменении \(\mu\) форма и размеры кеплеровой орбиты должны оставаться инвариантными.

Для энергетически оптимальной задачи с идеально регулируемой тягой и постоянной доступной мощностью используем первую форму уравнений оптимального движения:
\begin{equation}
	\dot{\mathbf r}
	=
	\mathbf v,
\end{equation}
\begin{equation}
	\dot{\mathbf v}
	=
	\frac{\bar\mu(\tau)}{\mu}
	\frac{\partial U}{\partial \mathbf r}
	+
	\mathbf p_v,
\end{equation}
\begin{equation}
	\dot{\mathbf p}_r
	=
	-
	\frac{\bar\mu(\tau)}{\mu}
	\frac{\partial^2 U}{\partial \mathbf r^2}
	\mathbf p_v,
\end{equation}
\begin{equation}
	\dot{\mathbf p}_v
	=
	-\mathbf p_r .
\end{equation}
Неизвестный начальный вектор сопряжённых переменных в методе продолжения запишем в виде
\begin{equation}
	\mathbf z_0
	=
	\begin{pmatrix}
		\mathbf p_{r0}\\
		\mathbf p_{v0}
	\end{pmatrix}.
\end{equation}
Задача Коши метода продолжения для \(\mathbf z_0(\tau)\) имеет вид
\begin{equation}
	\frac{d\mathbf z_0}{d\tau}
	=
	-
	\left(
	\frac{\partial \mathbf F}{\partial \mathbf z_0}
	\right)^{-1}
	\frac{\partial \mathbf F}{\partial \tau},
	\qquad
	\mathbf z_0(0)=\mathbf z_0^{\mathrm{init}} .
\end{equation}
Матрица \(\partial \mathbf F/\partial \mathbf z_0\) и вектор \(\partial \mathbf F/\partial \tau\) находятся из совместного интегрирования уравнений движения и уравнений в вариациях.

Для производных по \(\mathbf z_0=(\mathbf p_{r0},\mathbf p_{v0})\) используются уравнения
\begin{equation}
	\frac{d}{dt}
	\left(
	\frac{\partial \mathbf r}{\partial \mathbf z_0}
	\right)
	=
	\frac{\partial \mathbf v}{\partial \mathbf z_0},
\end{equation}
\begin{equation}
	\frac{d}{dt}
	\left(
	\frac{\partial \mathbf v}{\partial \mathbf z_0}
	\right)
	=
	\frac{\bar\mu(\tau)}{\mu}
	\frac{\partial^2 U}{\partial \mathbf r^2}
	\frac{\partial \mathbf r}{\partial \mathbf z_0}
	+
	\frac{\partial \mathbf p_v}{\partial \mathbf z_0},
\end{equation}
\begin{equation}
	\frac{d}{dt}
	\left(
	\frac{\partial \mathbf p_r}{\partial \mathbf z_0}
	\right)
	=
	-
	\frac{\bar\mu(\tau)}{\mu}
	\left[
	\frac{\partial}{\partial \mathbf r}
	\left(
	\frac{\partial^2 U}{\partial \mathbf r^2}\mathbf p_v
	\right)
	\frac{\partial \mathbf r}{\partial \mathbf z_0}
	+
	\frac{\partial^2 U}{\partial \mathbf r^2}
	\frac{\partial \mathbf p_v}{\partial \mathbf z_0}
	\right],
\end{equation}
\begin{equation}
	\frac{d}{dt}
	\left(
	\frac{\partial \mathbf p_v}{\partial \mathbf z_0}
	\right)
	=
	-
	\frac{\partial \mathbf p_r}{\partial \mathbf z_0}.
\end{equation}

Для производных по параметру продолжения \(\tau\) имеем
\begin{equation}
	\frac{d}{dt}
	\left(
	\frac{\partial \mathbf r}{\partial \tau}
	\right)
	=
	\frac{\partial \mathbf v}{\partial \tau},
\end{equation}
\begin{equation}
	\frac{d}{dt}
	\left(
	\frac{\partial \mathbf v}{\partial \tau}
	\right)
	=
	\left(1-\frac{\mu_0}{\mu}\right)
	\frac{\partial U}{\partial \mathbf r}
	+
	\frac{\bar\mu(\tau)}{\mu}
	\frac{\partial^2 U}{\partial \mathbf r^2}
	\frac{\partial \mathbf r}{\partial \tau}
	+
	\frac{\partial \mathbf p_v}{\partial \tau},
\end{equation}
\begin{equation}
	\frac{d}{dt}
	\left(
	\frac{\partial \mathbf p_r}{\partial \tau}
	\right)
	=
	-
	\left(1-\frac{\mu_0}{\mu}\right)
	\frac{\partial^2 U}{\partial \mathbf r^2}
	\mathbf p_v
	-
	\frac{\bar\mu(\tau)}{\mu}
	\left[
	\frac{\partial}{\partial \mathbf r}
	\left(
	\frac{\partial^2 U}{\partial \mathbf r^2}\mathbf p_v
	\right)
	\frac{\partial \mathbf r}{\partial \tau}
	+
	\frac{\partial^2 U}{\partial \mathbf r^2}
	\frac{\partial \mathbf p_v}{\partial \tau}
	\right],
\end{equation}
\begin{equation}
	\frac{d}{dt}
	\left(
	\frac{\partial \mathbf p_v}{\partial \tau}
	\right)
	=
	-
	\frac{\partial \mathbf p_r}{\partial \tau}.
\end{equation}

Начальные условия для основной системы задаются как
\begin{equation}
	\mathbf r(t_0)=\mathbf r_0,
	\qquad
	\mathbf v(t_0)=\mathbf v_0\sqrt{\frac{\bar\mu(\tau)}{\mu}},
\end{equation}
\begin{equation}
	\mathbf p_r(t_0)=\mathbf p_{r0},
	\qquad
	\mathbf p_v(t_0)=\mathbf p_{v0}.
\end{equation}

Для уравнений в вариациях по \(\mathbf z_0=(\mathbf p_{r0},\mathbf p_{v0})\) начальные условия имеют вид
\begin{equation}
	\frac{\partial \mathbf r(t_0)}{\partial \mathbf p_{r0}}=0,
	\qquad
	\frac{\partial \mathbf v(t_0)}{\partial \mathbf p_{r0}}=0,
	\qquad
	\frac{\partial \mathbf p_r(t_0)}{\partial \mathbf p_{r0}}=I_3,
	\qquad
	\frac{\partial \mathbf p_v(t_0)}{\partial \mathbf p_{r0}}=0,
\end{equation}
\begin{equation}
	\frac{\partial \mathbf r(t_0)}{\partial \mathbf p_{v0}}=0,
	\qquad
	\frac{\partial \mathbf v(t_0)}{\partial \mathbf p_{v0}}=0,
	\qquad
	\frac{\partial \mathbf p_r(t_0)}{\partial \mathbf p_{v0}}=0,
	\qquad
	\frac{\partial \mathbf p_v(t_0)}{\partial \mathbf p_{v0}}=I_3.
\end{equation}

Для производных по \(\tau\):
\begin{equation}
	\frac{\partial \mathbf r(t_0)}{\partial \tau}=0,
	\qquad
	\frac{\partial \mathbf v(t_0)}{\partial \tau}
	=
	\frac{1}{2}
	\sqrt{\frac{\mu}{\bar\mu(\tau)}}
	\left(1-\frac{\mu_0}{\mu}\right)
	\mathbf v_0,
\end{equation}
\begin{equation}
	\frac{\partial \mathbf p_r(t_0)}{\partial \tau}=0,
	\qquad
	\frac{\partial \mathbf p_v(t_0)}{\partial \tau}=0.
\end{equation}

После интегрирования расширенной системы до \(t_f\) вычисляются
\begin{equation}
	\frac{\partial \mathbf F}{\partial \mathbf z_0}
	=
	\begin{pmatrix}
		\dfrac{\partial \mathbf r(t_f)}{\partial \mathbf p_{r0}} &
		\dfrac{\partial \mathbf r(t_f)}{\partial \mathbf p_{v0}}\\[1.0ex]
		\dfrac{\partial \mathbf v(t_f)}{\partial \mathbf p_{r0}} &
		\dfrac{\partial \mathbf v(t_f)}{\partial \mathbf p_{v0}}
	\end{pmatrix},
\end{equation}
\begin{equation}
	\frac{\partial \mathbf F}{\partial \tau}
	=
	\begin{pmatrix}
		\dfrac{\partial \mathbf r(t_f)}{\partial \tau}\\[1.0ex]
		\dfrac{\partial \mathbf v(t_f)}{\partial \tau}
		-
		\dfrac{1}{2}
		\sqrt{\dfrac{\mu}{\bar\mu(\tau)}}
		\left(1-\dfrac{\mu_0}{\mu}\right)\mathbf v_f
	\end{pmatrix}.
\end{equation}
Так задаётся дифференциальное уравнение для \(\mathbf z_0(\tau)\). При \(\tau=1\) получается решение исходной задачи с реальным значением гравитационного параметра \(\mu\).

В вычислительном эксперименте для каждой даты старта метод продолжения по гравитационному параметру запускался для нескольких значений \(N_{\mathrm{br}}\). Для сравнения выбиралось лучшее найденное решение. Это необходимо, поскольку \(N_{\mathrm{br}}\) определяет ветвь продолжения, а заранее заданное значение \(N_{\mathrm{br}}\) не является частью исходной постановки задачи баллистического проектирования.

На рис.~\ref{fig:ch4_comparison_solution_paths_q_p_p} показаны пути численного решения для одного расчётного примера. Верхняя панель соответствует пути предлагаемого метода в пространстве управляющих параметров \(\mathbf q\). Средняя панель показывает индуцированный этим изменением путь начального вектора сопряжённых переменных. Нижняя панель соответствует пути начального вектора сопряжённых переменных при продолжении по гравитационному параметру.

\begin{figure}[ht]
	\centering
	\includegraphics[width=\textwidth]{images/comparison_solution_paths_q_p_p.png}
	\caption{Сравнение путей численного решения: путь предлагаемого метода в пространстве управляющих параметров \(\mathbf q\), индуцированный путь начального вектора сопряжённых переменных и путь начального вектора сопряжённых переменных при продолжении по гравитационному параметру.}
	\label{fig:ch4_comparison_solution_paths_q_p_p}
\end{figure}

Из рис.~\ref{fig:ch4_comparison_solution_paths_q_p_p} видно, что продолжение по гравитационному параметру задаёт гладкий путь по параметру \(\tau\), тогда как предлагаемый метод изменяет управляющие параметры итерационно при решении системы невязок. При этом переменные \(\mathbf q\) имеют непосредственную баллистическую интерпретацию: изменение \(\lambda\) и \(\kappa\) соответствует переходу по аппроксимированному модельному многообразию, а изменение \(\zeta,\chi,j,\gamma\) соответствует уточнению параметров \(R\)-преобразований.

Сравнение проводилось на сетке из ста дат старта для перелёта к Марсу с шагом в 15 дней начиная с даты 2026.01.01. В корректной реализации оба метода доводят невязки правого конца до уровня машинной точности; поэтому в таблице качества приведены число расчётов и доля успешных завершений.

\begin{table}[ht]
	\centering
	\caption{Сводное сравнение успешности расчётов на сетке дат старта.}
	\label{tab:ch4_date_sweep_quality}
	\begin{tabular}{lcc}
		\toprule
		Метод & Число расчётов & Доля успешных \\
		\midrule
		Предлагаемый подход & \(100\) & \(100\%\) \\
		Продолжение по \(\mu\) & \(100\) & \(100\%\) \\
		\bottomrule
	\end{tabular}
\end{table}

В табл.~\ref{tab:ch4_date_sweep_speed} приведены основные характеристики вычислительных затрат. Вычисления выполнялись на одном процессоре с использованием языка Python. Для предлагаемого подхода указано среднее число интегрирований при уточнении \(\mathbf q\). Для продолжения по гравитационному параметру дополнительно указано среднее число перебранных ветвей \(N_{\mathrm{br}}\).

\begin{table}[ht]
	\centering
	\caption{Сводное сравнение вычислительных затрат на сетке дат старта.}
	\label{tab:ch4_date_sweep_speed}
	\begin{tabular}{@{}lccc@{}}
		\toprule
		Метод
		& \shortstack{Среднее\\время, с}
		& \shortstack{Среднее число\\интегрирований}
		& \shortstack{Среднее число\\проверенных ветвей} \\
		\midrule
		Предлагаемый подход & \(\phantom{-}6.36\times10^{-1}\) & \(\phantom{-}7.03\times10^{1\phantom{-}}\) & -- \\
		Продолжение по \(\mu\) & \(\phantom{-}1.89\times10^{0\phantom{-}}\) & \(\phantom{-}4.30\times10^{1\phantom{-}}\) & \(\phantom{-}3.80\times10^{0\phantom{-}}\) \\
		\bottomrule
	\end{tabular}
\end{table}

В рассматриваемой серии расчётов продолжение по гравитационному параметру успешно находило решение для каждой даты старта, однако требовало дополнительного выбора ветви. Лучшая ветвь менялась по сетке дат старта: для части дат выбиралось \(N_{\mathrm{br}}=1\), для части --- \(N_{\mathrm{br}}=0\). Это показывает, что номер ветви не является заранее известным параметром в массовом проектном расчёте.

Таким образом, проведённое сравнение показывает, что оба подхода применимы для уточнения траекторий в задаче встречи. Отличие предлагаемого подхода состоит в том, что уточнение выполняется непосредственно в пространстве управляющих параметров \(\mathbf q\), связанных с конечными формулами и \(R\)-преобразованиями. Поэтому метод не требует отдельного задания номера ветви продолжения и является более прямым способом перехода от аналитического начального приближения к численно уточнённой траектории.


\section{Постановки задачи с ненулевой отлётной скоростью}
\label{sec:ch4_nonzero_departure_velocity}

В предыдущих разделах начальное приближение строилось для задачи встречи, в которой начальный фазовый вектор космического аппарата совпадает с фазовым вектором тела отправления. Такая постановка соответствует нулевому гиперболическому избытку скорости относительно тела отправления. В прикладном баллистическом проектировании это ограничение не всегда выполняется: разгонный блок или предварительный участок выведения может сообщать аппарату ненулевую отлётную скорость. Поэтому рассмотрим обобщение постановки, в котором начальная скорость аппарата имеет вид
\begin{equation}
	\mathbf v_0=\mathbf v_C+\mathbf v_\infty .
\end{equation}
Здесь \(\mathbf v_C\) обозначает скорость тела отправления в выбранной модельной или эфемеридной постановке, а \(\mathbf v_\infty\) --- вектор гиперболического избытка скорости. Величину
\begin{equation}
	v_\infty=\|\mathbf v_\infty\|
\end{equation}
будем называть отлётной скоростью.

При ненулевом \(\mathbf v_\infty\) естественно возникают три близкие, но различные постановки краевой задачи. Они отличаются тем, какие компоненты начальной скорости считаются заданными, а какие могут выбираться в процессе оптимизации.

В первой постановке вектор отлётной скорости фиксирован:
\begin{equation}
	\mathbf v_\infty=\mathbf v_\infty^*.
\end{equation}
Тогда начальная скорость полностью задана, и условия трансверсальности на левом конце для сопряжённой скорости не возникают:
\begin{equation}
	\mathbf p_v(t_0)\in\mathbb R^n .
\end{equation}
Эта постановка является наиболее простой с точки зрения численного продолжения: для каждого выбранного значения \(\mathbf v_\infty^*\) решается обычная задача встречи с фиксированным начальным фазовым вектором.

Во второй постановке фиксируется только величина отлётной скорости:
\begin{equation}
	\|\mathbf v_\infty\|=v_\infty^*,
\end{equation}
а направление \(\mathbf v_\infty\) должно быть выбрано оптимально. В этом случае допустимые вариации начальной скорости лежат в касательном пространстве к окружности или сфере постоянного \(v_\infty\). Поэтому вектор \(\mathbf p_v(t_0)\) должен быть направлен по нормали к этой поверхности:
\begin{equation}
	\mathbf p_v(t_0)\parallel \mathbf v_\infty .
\end{equation}
В плоском случае это условие можно записать в виде
\begin{equation}
	p_{vy}(t_0)v_{\infty x}
	-
	p_{vx}(t_0)v_{\infty y}
	=0 .
\end{equation}
Именно эта постановка соответствует поиску оптимального направления отлёта при заданном энергетическом уровне выведения.

В третьей постановке вектор \(\mathbf v_\infty\) считается свободным:
\begin{equation}
	\mathbf v_\infty\in\mathbb R^n .
\end{equation}
Так как начальная скорость в этом случае не фиксируется, условие трансверсальности принимает вид
\begin{equation}
	\mathbf p_v(t_0)=0 .
\end{equation}
Эта постановка задаёт предельный случай, в котором отлётная скорость выбирается самой задачей оптимизации.

Связь между тремя постановками поясняется через поверхность значений функционала, построенную по решениям первой задачи при разных фиксированных компонентах \(\mathbf v_\infty\). Пусть
\begin{equation}
	J_1(v_{\infty x},v_{\infty y})
\end{equation}
обозначает значение функционала для задачи с фиксированным вектором отлётной скорости. Тогда задача с фиксированной величиной \(v_\infty\) соответствует поиску минимума этой поверхности на окружности
\begin{equation}
	v_{\infty x}^2+v_{\infty y}^2=(v_\infty^*)^2 .
\end{equation}
При изменении \(v_\infty^*\) такие точки формируют кривую локальных минимумов. Свободная постановка соответствует точке наименьшего значения функционала на построенной поверхности \(J_1\).

\begin{figure}[htbp]
	\centering
	\includegraphics[width=0.72\textwidth]{images/GlobalLocalVhyp.png}
	\caption{Изолинии функционала на плоскости компонент отлётной скорости. Зелёной линией показана кривая локальных минимумов для задачи с фиксированной величиной отлётной скорости, красной точкой --- решение задачи со свободной отлётной скоростью.}
	\label{fig:ch4_global_local_vhyp}
\end{figure}

Рис.~\ref{fig:ch4_global_local_vhyp} показывает, что задача с фиксированной величиной отлётной скорости не является независимой от задачи с фиксированным вектором \(\mathbf v_\infty\). Её можно рассматривать как ограниченную минимизацию по поверхности \(J_1(v_{\infty x},v_{\infty y})\). При этом решение свободной задачи задаёт естественную предельную точку: если ограничение на величину \(v_\infty\) не накладывается, оптимизация выбирает не только направление, но и саму величину гиперболического избытка.

Для дальнейшего использования в баллистическом проектировании важна не только структура поверхности по компонентам отлётной скорости, но и зависимость оптимального значения \(v_\infty\) от витковой дальности. Поэтому дополнительно рассмотрим семейство задач, в котором для каждой витковой дальности \(\lambda\) и величины отлётной скорости \(v_\infty\) решается задача с фиксированным модулем гиперболического избытка. Полученная поверхность функционала позволяет определить кривую минимумов по \(v_\infty\).

\begin{figure}[htbp]
	\centering
	\includegraphics[width=0.72\textwidth]{images/Vinf_lambda_functional_isolines.png}
	\caption{Изолинии функционала на плоскости витковой дальности и величины отлётной скорости. Красной линией показана кривая минимумов по \(v_\infty\) при фиксированной витковой дальности.}
	\label{fig:ch4_vinf_lambda_functional_isolines}
\end{figure}

Из рис.~\ref{fig:ch4_vinf_lambda_functional_isolines} видно, что оптимальная величина отлётной скорости меняется сравнительно плавно при изменении витковой дальности. При этом кривая минимумов проходит в области умеренных значений \(v_\infty\), а не при \(v_\infty=0\). Это подтверждает, что постановка с ненулевой отлётной скоростью не является малой технической поправкой к исходной задаче встречи: она меняет положение локально наилучшего решения в пространстве параметров и должна учитываться отдельно.

Для использования в алгоритме начального приближения наиболее важна не вся поверхность \(J_1\), а возможность связать задачу с нулевым гиперболическим избытком и задачу с ненулевой отлётной скоростью. В качестве рабочей схемы можно использовать однопараметрическое продолжение
\begin{equation}
	v_\infty(s)=s v_\infty^*,
	\qquad
	s\in[0,1],
\end{equation}
где при \(s=0\) получается постановка с нулевой отлётной скоростью, а при \(s=1\) --- требуемая постановка с заранее неизвестной величиной \(v_\infty^*\). На каждом шаге такого продолжения решается задача второго типа, то есть вектор гиперболического избытка направлен по \(\mathbf p_v(t_0)\). Продолжение выполняется либо до достижения оптимального значения, либо до максимально возможного гиперболического избытка. Использование продолжения решает проблему с \(\mathbf p_v(t_0) = \mathbf 0\) в точке оптимального гиперболического избытка скорости.

Тем самым постановки с ненулевой отлётной скоростью образуют надстройку над алгоритмом, описанным в предыдущих разделах. Конечные формулы и \(R\)-преобразования дают начальный вектор расширенной системы для случая \(\mathbf v_\infty=0\), после чего продолжение по \(\mathbf v_\infty\) позволяет перейти к решению с оптимальным гиперболическим избытком. Такой переход важен для прикладных миссий, где межпланетный участок с непрерывной тягой начинается не с точного фазового вектора тела отправления, а после предварительного выведения аппарата на отлётную траекторию.

\section{Применение алгоритма при проектировании миссии Земля--Венера}
\label{sec:ch4_venus_mission_application}

Алгоритм построения начального приближения, его проверка для перелётов Земля--Марс и Земля--Венера, а также постановки задачи с ненулевой отлётной скоростью объединяются в прикладную схему проектирования межпланетной миссии. В качестве примера рассматривается перелёт Земля--Венера для малого космического аппарата с электрореактивной двигательной установкой.

В разделе рассматривается вычислительная цепочка, в которой результаты настоящей работы используются как средство получения и переноса начальных приближений. Сначала строится приближение в модели идеально регулируемой тяги, поскольку эта постановка приводит к гладкому управлению и удобна для получения исходного решения. Затем с помощью продолжения по допустимой величине гиперболического избытка выполняется переход к постановке с ненулевой отлётной скоростью. После этого решение уточняется в модели постоянной скорости истечения, где оптимальный профиль тяги имеет структуру с активными и пассивными участками. Полученное опорное решение далее переносится на другие даты старта с помощью \(R\)-преобразования I типа и используется как начальное приближение для массового расчёта.

Для межпланетного участка в модели постоянной скорости истечения минимизируется расход рабочего тела:
\begin{equation}
	J_{\text{ПСИ}}
	=
	\int_{t_0}^{t_0+T} \dot m_p(t)\,dt
	\rightarrow
	\min ,
\end{equation}
где \(\dot m_p\) обозначает скорость расхода рабочего тела, \(t_0\) --- дату старта межпланетного участка, а \(T\) --- его продолжительность. Начальная скорость космического аппарата записывается как сумма скорости Земли и вектора гиперболического избытка:
\begin{equation}
	\mathbf v(t_0)
	=
	\mathbf v_E(t_0)
	+
	\mathbf v_\infty .
	\label{eq:ch4_venus_initial_velocity_with_vinf}
\end{equation}

В модели постоянной скорости истечения оптимальное управление имеет структуру bang-bang. Направление тяги определяется вектором, сопряжённым скорости:
\begin{equation}
	\mathbf e
	=
	\frac{\mathbf p_v}{\|\mathbf p_v\|}.
\end{equation}
Активные и пассивные участки задаются функцией переключения. В используемой записи её можно представить в виде
\begin{equation}
	\mathcal K
	=
	\frac{\nu\|\mathbf p_v\|}{m(t)}
	+
	p_m
	-
	1 ,
\end{equation}
где \(\nu\) --- эффективная скорость истечения, \(m(t)\) --- текущая масса аппарата, а \(p_m\) --- переменная, сопряжённая массе. Тогда расход рабочего тела принимает одно из двух значений:
\begin{equation}
	\dot m_p
	=
	\begin{cases}
		\dot m_{\max}, & \mathcal K>0,\\
		0, & \mathcal K\leq 0.
	\end{cases}
\end{equation}
Тем самым переход от ИРТ-приближения к ПСИ-постановке сопровождается появлением пассивных участков, что существенно усложняет прямое получение сходящегося начального приближения.

С вычислительной точки зрения переход от ИРТ-приближения к опорному ПСИ-решению выполняется как последовательность связанных задач. На всех этапах этого перехода граничные условия на правом конце траектории
\begin{equation}
	\mathbf r(t_0+T)=\mathbf r_V(t_0+T),
	\qquad
	\mathbf v(t_0+T)=\mathbf v_V(t_0+T)
\end{equation}
сохраняются в составе ограничений типа равенства. В используемой реализации метода внутренней точки эти условия входят в систему ограничений оптимизационной задачи и удерживаются при переходе от одного приближения к следующему.

Ненулевой гиперболический избыток при отлёте от Земли вводится иначе. Он добавляется плавно через продолжение по ограничению типа неравенства:
\begin{equation}
	\|\mathbf v_\infty\|
	\leq
	v_{\infty,\max}(s),
	\qquad
	s\in[0,1],
\end{equation}
где
\begin{equation}
	v_{\infty,\max}(s)
	=
	4s\ \text{км/с}.
\end{equation}
При \(s=0\) допустимая величина гиперболического избытка равна нулю, что соответствует исходной постановке без отлётной скорости. При \(s=1\) максимально допустимая величина гиперболического избытка достигает \(4\) км/с. Таким образом, область допустимых значений \(\mathbf v_\infty\) раскрывается постепенно, тогда как выполнение граничных условий на правом конце сохраняется на всём процессе продолжения.

После такого перехода было получено опорное решение перелёта Земля--Венера с датой старта 01.11.2032. Траектория показана на рис.~\ref{fig:ch4_venus_reference_trajectory}, а основные параметры приведены в табл.~\ref{tab:ch4_venus_reference_solution}.

\begin{figure}[htbp]
	\centering
	\includegraphics[width=0.78\textwidth]{images/Venus_initial_guess_before_transfer.png}
	\caption{Опорная траектория перелёта Земля--Венера с датой старта 01.11.2032 и длительностью \(390.8\) суток.}
	\label{fig:ch4_venus_reference_trajectory}
\end{figure}

\begin{table}[htbp]
	\centering
	\caption{Параметры опорного перелёта Земля--Венера.}
	\label{tab:ch4_venus_reference_solution}
	\begin{tabular}{lc}
		\toprule
		Параметр & Значение \\
		\midrule
		Затраты топлива & \(41.77\) кг \\
		Затраты характеристической скорости & \(3.18\) км/с \\
		Время перелёта & \(390.8\) суток \\
		Гиперболический избыток при отлёте & \(2.67\) км/с \\
		Моторное время & \(195.2\) суток \\
		Дата старта & 01.11.2032 \\
		Начальная масса КА & \(156\) кг \\
		Первый активный участок & 31.12.2032--28.05.2033 \\
		Второй активный участок & 09.10.2033--25.11.2033 \\
		\bottomrule
	\end{tabular}
\end{table}

Полученное опорное решение может быть использовано для инициализации расчётов с другими датами старта. Пусть \((\mathbf r_0,\mathbf v_0)\) --- начальный фазовый вектор опорного решения, а \((\mathbf r'_0,\mathbf v'_0)\) --- начальный фазовый вектор для новой даты старта. Используя матрицу \(R\)-преобразования I типа, введённую в главе 3, запишем матрицу перехода в виде
\begin{equation}
	G
	=
	R_0(\mathbf r'_0,\mathbf v'_0)
	R_0^{-1}(\mathbf r_0,\mathbf v_0).
	\label{eq:ch4_venus_G_transfer}
\end{equation}
Эта матрица связывает локальные начальные конфигурации двух задач и задаёт перенос как начальных сопряжённых переменных, так и вектора гиперболического избытка.

В соответствии с правилом преобразования сопряжённых переменных получаем
\begin{equation}
	\mathbf p'_r(t_0)
	=
	(G^T)^{-1}\mathbf p_r(t_0),
	\qquad
	\mathbf p'_v(t_0)
	=
	(G^T)^{-1}\mathbf p_v(t_0).
	\label{eq:ch4_venus_costate_transfer}
\end{equation}
В рассматриваемой постановке со свободным гиперболическим избытком при отлёте выполняется условие трансверсальности
\begin{equation}
	\mathbf p_v(t_0)=\mathbf 0,
\end{equation}
поэтому оно сохраняется при преобразовании автоматически.

Массовая сопряжённая переменная и продолжительность перелёта при первичном переносе опорного решения не изменяются:
\begin{equation}
	p'_m(t_0)=p_m(t_0),
	\qquad
	T'=T .
	\label{eq:ch4_venus_pm_T_transfer}
\end{equation}
Вектор гиперболического избытка при отлёте переносится тем же линейным преобразованием, что и начальная скорость:
\begin{equation}
	\mathbf v'_\infty
	=
	G\mathbf v_\infty .
	\label{eq:ch4_venus_vinf_transfer}
\end{equation}
Эта формула важна для практического применения метода: при переходе к другой дате старта изменяется не только начальный вектор сопряжённых переменных, но и начальное предположение для отлётной скорости.

Так как матрица \(G\) в общем случае не является ортогональной, преобразование \eqref{eq:ch4_venus_vinf_transfer} не сохраняет величину гиперболического избытка. Если
\begin{equation}
	v_\infty=\|\mathbf v_\infty\|,
	\qquad
	v'_\infty=\|\mathbf v'_\infty\|,
\end{equation}
то после переноса
\begin{equation}
	v'_\infty
	=
	\|G\mathbf v_\infty\|
	=
	\sqrt{
		\mathbf v_\infty^T
		G^T G
		\mathbf v_\infty
	}.
	\label{eq:ch4_venus_vinf_norm_deformation}
\end{equation}
Вводя единичный вектор направления гиперболического избытка
\begin{equation}
	\mathbf n_\infty
	=
	\frac{\mathbf v_\infty}{\|\mathbf v_\infty\|},
\end{equation}
получаем относительную деформацию его величины:
\begin{equation}
	\frac{v'_\infty}{v_\infty}
	=
	\sqrt{
		\mathbf n_\infty^T
		G^T G
		\mathbf n_\infty
	}.
	\label{eq:ch4_venus_vinf_relative_deformation}
\end{equation}
Следовательно, перенос гиперболического избытка является не только изменением направления отлёта, но и изменением его величины. Это согласуется с общей ролью \(R\)-преобразования I типа: при переходе между разными начальными фазовыми векторами меняется локальная геометрия задачи.

Полная формула переноса опорного решения на новую дату старта имеет вид
\begin{equation}
	\begin{aligned}
		\mathbf p'_r(t_0)
		&=
		(G^T)^{-1}\mathbf p_r(t_0),\\
		\mathbf p'_v(t_0)
		&=
		(G^T)^{-1}\mathbf p_v(t_0),\\
		p'_m(t_0)
		&=
		p_m(t_0),\\
		\mathbf v'_\infty
		&=
		G\mathbf v_\infty,\\
		T'
		&=
		T .
	\end{aligned}
	\label{eq:ch4_venus_reference_solution_transfer}
\end{equation}
После такого переноса полученный начальный вектор используется как начальное приближение в задаче оптимизации для новой даты старта. На рис.~\ref{fig:ch4_venus_initial_guess_transfer} показан пример переноса опорного решения для даты старта 01.11.2032 на дату 01.01.2028.

\begin{figure}[htbp]
	\centering
	\begin{minipage}{0.49\textwidth}
		\centering
		\includegraphics[width=\textwidth]{images/Venus_initial_guess_before_transfer.png}
	\end{minipage}
	\hfill
	\begin{minipage}{0.49\textwidth}
		\centering
		\includegraphics[width=\textwidth]{images/Venus_initial_guess_after_transfer.png}
	\end{minipage}
	\caption{Перенос опорного решения на новую дату старта: исходное решение для 01.11.2032 и начальное приближение для 01.01.2028 после применения преобразования \eqref{eq:ch4_venus_reference_solution_transfer}.}
	\label{fig:ch4_venus_initial_guess_transfer}
\end{figure}

Эта процедура использована для массового расчёта по датам старта. Рассматривался интервал от 01.01.2028 до 01.01.2037 с шагом в один день. Для каждой даты начальное приближение строилось по формуле \eqref{eq:ch4_venus_reference_solution_transfer}, после чего уточнялось методом внутренней точки. Таким образом, после получения одного опорного решения дальнейший перебор дат старта не требует повторного выполнения всей цепочки продолжения от ИРТ-приближения.

На рис.~\ref{fig:ch4_venus_delta_v_start_dates} показана зависимость затрат характеристической скорости на межпланетном участке от даты старта. Она имеет выраженную оконную структуру: локально благоприятные даты повторяются с периодом, близким к синодическому периоду Венеры, а наиболее выгодные даты проявляются на интервале порядка периода великого противостояния Земли и Венеры.

\begin{figure}[htbp]
	\centering
	\includegraphics[width=0.82\textwidth]{images/Venus_deltaV_by_start_date.png}
	\caption{Зависимость затрат характеристической скорости на межпланетном участке перелёта Земля--Венера от даты старта.}
	\label{fig:ch4_venus_delta_v_start_dates}
\end{figure}

На рис.~\ref{fig:ch4_venus_vinf_start_dates} показана зависимость гиперболического избытка при отлёте от Земли от даты старта. Эта величина определяет требования к разгонному блоку при выводе космического аппарата на отлётную траекторию.

\begin{figure}[htbp]
	\centering
	\includegraphics[width=0.82\textwidth]{images/Venus_vinf_by_start_date.png}
	\caption{Зависимость гиперболического избытка при отлёте от Земли от даты старта.}
	\label{fig:ch4_venus_vinf_start_dates}
\end{figure}

На рис.~\ref{fig:ch4_venus_tof_start_dates} приведена зависимость продолжительности межпланетного участка от даты старта. Совместно с затратами характеристической скорости и величиной гиперболического избытка эта зависимость позволяет выбирать компромиссные варианты перелёта с учётом ограничений миссии.

\begin{figure}[htbp]
	\centering
	\includegraphics[width=0.82\textwidth]{images/Venus_tof_by_start_date.png}
	\caption{Зависимость продолжительности межпланетного участка перелёта Земля--Венера от даты старта.}
	\label{fig:ch4_venus_tof_start_dates}
\end{figure}

Полученные результаты показывают, что \(R\)-преобразование I типа может использоваться не только для построения начального приближения в одной эфемеридной конфигурации, но и для быстрого переноса уже найденного решения по сетке дат старта. При этом вместе с начальными сопряжёнными переменными переносится вектор гиперболического избытка скорости, а его величина изменяется в соответствии с деформацией \eqref{eq:ch4_venus_vinf_norm_deformation}. В результате формируется практическая схема массового баллистического расчёта: одно опорное решение используется как источник начальных приближений для целого интервала дат старта, после чего каждое приближение уточняется численным методом.

Таким образом, на примере миссии Земля--Венера показано, как конечные формулы и \(R\)-преобразования применяются в прикладном проектировании. Сначала они дают ИРТ-приближение для гладкой энергетически оптимальной задачи, затем это приближение используется при продолжении к ненулевому гиперболическому избытку и ПСИ-модели, после чего найденное опорное решение переносится на другие даты старта. Это позволяет получить картину пролётных окон для перелёта с непрерывной тягой без построения полного семейства решений заново для каждой даты.

\section{Сценарии использования и программная реализация}

Результаты, полученные в предыдущих разделах, образуют не только способ построения отдельного начального приближения, но и вычислительную схему, пригодную для использования в проектно-баллистическом анализе. Основным интерфейсом этой схемы является шестёрка управляющих параметров
\[
\mathbf q=(\lambda,\kappa,\zeta,\chi,j,\gamma).
\]
Первые два параметра определяют положение на аппроксимированном модельном многообразии, а остальные четыре задают действие \(R\)-преобразований, учитывающих эллиптичность и пространственную ориентацию конечной орбиты. Благодаря этому построение начального вектора расширенной системы сводится к последовательному вычислению конечных формул и применению линейных преобразований начальных значений сопряжённых переменных.

Такое представление отделяет геометрический смысл приближения от численных деталей последующего уточнения. Пользователь задаёт физическую постановку перелёта: дату старта, начальное небесное тело, конечное небесное тело или целевую орбиту, а также параметры отлётной скорости, если рассматривается постановка с ненулевым гиперболическим избытком. Далее вычисляются параметры \(\mathbf q\), по ним строится ИРТ-приближение, после чего оно может использоваться как начальный вектор для уточнения энергетически оптимальной траектории или как опорное решение для более сложной расчётной цепочки.

В общем виде вычислительная схема включает следующие этапы:
\begin{enumerate}
	\item задание даты старта, начального фазового вектора и целевых параметров конечной орбиты;
	\item вычисление модельных параметров \(\lambda\) и \(\kappa\);
	\item вычисление продолжительности перелёта, значения функционала и начального вектора сопряжённых переменных по конечным формулам аппроксимированного многообразия;
	\item вычисление управляющих параметров \(\zeta,\chi,j,\gamma\) для задания элементов конечной орбиты;
	\item применение композиции \(R\)-преобразований и получение начального вектора расширенной системы;
	\item численное уточнение траектории в переменных \(\mathbf q\);
	\item при необходимости, продолжение по \(\mathbf v_\infty\) и переход к ПСИ-модели.
\end{enumerate}

Тем самым параметры \(\mathbf q\) используются не только как способ компактной записи начального приближения, но и как переменные последующего уточнения. Вместо прямого варьирования шести начальных значений сопряжённых переменных используется набор параметров, каждый из которых связан с определённой частью построения траектории. Параметры \(\lambda\) и \(\kappa\) отвечают за выбор модельного решения, параметры \(\zeta\) и \(\chi\) связаны с плоской деформацией конечной орбиты, а параметры \(j\) и \(\gamma\) отвечают за пространственную деформацию.

Основные сценарии использования метода приведены в табл.~\ref{tab:ch4_usage_scenarios}.

\begin{table}[ht]
	\centering
	\caption{Основные сценарии использования разработанного метода.}
	\label{tab:ch4_usage_scenarios}
	\begin{tabular}{p{0.39\textwidth}p{0.52\textwidth}}
		\toprule
		Сценарий & Результат \\
		\midrule
		Построение начального приближения & Начальный вектор расширенной системы для энергетически оптимальной задачи встречи \\
		Перенос между датами старта & Новое приближение, полученное из опорного решения с помощью \(R\)-преобразования I типа \\
		Уточнение в параметрах \(\mathbf q\) & Локально оптимальная траектория, удовлетворяющая граничным условиям задачи встречи \\
		Учёт ненулевой отлётной скорости & Решение с заданным или оптимизируемым гиперболическим избытком \(\mathbf v_\infty\) \\
		Переход к ПСИ-модели & Опорная траектория с разрывным профилем тяги, построенная на основе гладкого ИРТ-приближения \\
		Массовый расчёт по датам старта & Оценка окон старта, продолжительности перелёта, затрат характеристической скорости и расхода рабочего тела \\
		\bottomrule
	\end{tabular}
\end{table}

Первый сценарий является базовым. В этом случае метод используется для получения начального приближения без решения вспомогательной краевой задачи. По дате старта и целевым орбитальным параметрам вычисляется вектор \(\mathbf q\), затем строится начальный вектор расширенной системы. Такое приближение не является конечным результатом расчёта, но оно оказывается достаточно близким к искомой экстремали, чтобы использовать его в последующем численном уточнении.

Второй сценарий связан с изменением даты старта. При массовом проектном расчёте близкие даты старта часто требуют решения близких краевых задач, однако из-за чувствительности расширенной системы прямое использование уже найденного решения не всегда устойчиво. \(R\)-преобразование I типа позволяет согласованно изменить начальный фазовый вектор и начальный вектор сопряжённых переменных. Поэтому опорное решение можно переносить на соседние даты старта не как простой поворот или сдвиг траектории, а как преобразование начального вектора расширенной системы.

Третий сценарий состоит в уточнении траектории непосредственно в пространстве параметров \(\mathbf q\). В этом случае конечные формулы используются для построения первого приближения, а дальнейшая численная процедура изменяет \(\lambda,\kappa,\zeta,\chi,j,\gamma\), минимизируя невязки правого конца. Такая постановка сохраняет связь переменных оптимизации с геометрией перелёта и уменьшает произвольность выбора начального вектора сопряжённых переменных.

Четвёртый сценарий возникает при рассмотрении постановок с ненулевой отлётной скоростью. ИРТ-приближение, построенное для случая \(\mathbf v_\infty=0\), может использоваться как начальная точка для продолжения по гиперболическому избытку. Такой подход позволяет перейти от более простой гладкой постановки к задаче с ненулевой скоростью отлёта, не начиная решение краевой задачи заново.

Пятый сценарий связан с переходом от ИРТ-модели к ПСИ-модели. В этом случае гладкая энергетически оптимальная траектория используется как опорная для построения траектории с постоянной скоростью истечения и разрывным профилем тяги. При таком использовании метод не заменяет последующее уточнение в ПСИ-модели, а предоставляет согласованное начальное приближение, которое сохраняет выполнение основных граничных условий и облегчает дальнейший расчёт.

Шестой сценарий представляет наибольший прикладной интерес для проектно-баллистического анализа. После получения одного или нескольких опорных решений метод может применяться на сетке дат старта. Для каждой даты строится или переносится начальное приближение, затем выполняется уточнение, после чего оцениваются продолжительность перелёта, расход рабочего тела, величина гиперболического избытка и другие характеристики миссии. Именно в таком режиме метод становится инструментом предварительного выбора окон старта и сравнения вариантов перелёта.

Программная реализация разработанного подхода выполнена в виде комплекса ILTT Toolbox. Его назначение состоит в том, чтобы объединить конечные формулы, \(R\)-преобразования и численное уточнение в единую воспроизводимую вычислительную цепочку. При этом ILTT Toolbox не подменяет математическое содержание метода: конечные формулы, управляющие параметры и \(R\)-преобразования остаются основными результатами, а программная реализация обеспечивает их практическое применение.

ILTT Toolbox предоставляет следующий функционал:
\begin{itemize}
	\item вычисление модельных параметров \(\lambda\) и \(\kappa\) по постановке перелёта;
	\item вычисление продолжительности перелёта, значения функционала и начальных значений сопряжённых переменных по конечным формулам;
	\item построение матриц \(R\)-преобразований I, II и III типов;
	\item вычисление управляющих параметров \(\zeta,\chi,j,\gamma\) по целевым элементам конечной орбиты;
	\item применение композиции \(R\)-преобразований к начальному вектору сопряжённых переменных;
	\item интегрирование расширенной системы и вычисление невязок правого конца;
	\item уточнение траектории в параметрах \(\mathbf q\);
	\item построение расчётных цепочек с ненулевой отлётной скоростью и переходом к ПСИ-модели;
	\item выполнение серийных расчётов по датам старта для анализа окон перелёта.
\end{itemize}

На вход программной реализации подаются физические параметры постановки: дата старта, начальное и конечное небесные тела или соответствующие фазовые векторы, целевые параметры конечной орбиты, а также дополнительные параметры постановки с гиперболическим избытком. На выходе формируется начальный вектор расширенной системы, уточнённая траектория, значение функционала, продолжительность перелёта, невязки правого конца и прикладные характеристики миссии, необходимые для дальнейшего анализа.

ILTT Toolbox реализует как набор отдельных конечных формул, так и общую логику метода. Сначала выбирается точка на аппроксимированном модельном многообразии, затем выполняется деформация этой точки с помощью \(R\)-преобразований, после чего результат уточняется численно. Такая структура сохраняет физическую интерпретируемость расчёта: изменение каждого параметра \(\mathbf q\) соответствует изменению определённой части баллистической постановки.

Ограничения метода также имеют прикладное значение. Конечные формулы используются в области, где была выполнена их численная верификация; за пределами этой области требуется дополнительная проверка качества начального приближения. Кроме того, построенное ИРТ-приближение не является заменой финального расчёта траектории в более реалистичной модели движения и двигателя. Оно служит средством выбора начального вектора, переноса опорного решения и ускорения расчётной процедуры.

Таким образом, разработанный метод может использоваться на двух уровнях. На первом уровне он даёт быстрый способ построения начального приближения для отдельной энергетически оптимальной задачи встречи. На втором уровне он становится частью проектной вычислительной цепочки, в которой одно или несколько опорных решений используются для анализа множества дат старта, постановок с ненулевой отлётной скоростью и перехода к ПСИ-модели. Именно это делает управляющие параметры \(\mathbf q\) удобным связующим звеном между конечными формулами, непрямым численным уточнением и практическим баллистическим проектированием.

\section{Выводы к главе 4}

В настоящей главе рассмотрено применение конечных формул и \(R\)-преобразований к задаче баллистического проектирования межпланетных перелётов. Результаты второй и третьей глав объединены в прикладной алгоритм построения начального приближения.

Основные результаты главы состоят в следующем.

\begin{enumerate}
	\item Введена шестёрка управляющих параметров начального приближения
	\[
	\mathbf q=(\lambda,\kappa,\zeta,\chi,j,\gamma).
	\]
	Параметры \(\lambda\) и \(\kappa\) задают точку на аппроксимированном модельном многообразии, а параметры \(\zeta,\chi,j,\gamma\) определяют плоское и пространственное \(R\)-преобразования. Тем самым начальное приближение задаётся не непосредственно шестью начальными значениями сопряжённых переменных, а параметрами, связанными с геометрией перелёта и элементами конечной орбиты.
	
	\item Построен алгоритм получения начального вектора расширенной системы для энергетически оптимальной задачи встречи. Алгоритм использует конечные формулы главы~2 для вычисления продолжительности перелёта, функционала и начальных значений сопряжённых переменных, а затем применяет \(R\)-преобразования главы~3 для учёта эллиптичности и пространственной ориентации конечной орбиты. В результате формируется ИРТ-приближение, пригодное для последующего численного уточнения.
	
	\item На примерах перелётов Земля--Марс и Земля--Венера показано, что построенное начальное приближение воспроизводит основные целевые параметры конечной орбиты с приемлемой для последующего уточнения точностью. Численная проверка выполнена не для одной отдельной даты, а на наборах дат старта, соответствующих характерным периодам повторения баллистической геометрии. Это показывает, что алгоритм может использоваться как инструмент предварительного массового расчёта.
	
	\item Показано, что управляющие параметры \(\mathbf q\) могут использоваться как переменные численного уточнения. В такой постановке уточняется не произвольный начальный вектор сопряжённых переменных, а параметры, отвечающие за витковую дальность, отношение радиусов и действие \(R\)-преобразований. Это сохраняет геометрическую интерпретацию процесса и позволяет получать решения, удовлетворяющие граничным условиям задачи встречи.
	
	\item Проведено сравнение с методом продолжения по гравитационному параметру. Сравнение показало различие в роли двух подходов: продолжение по гравитационному параметру строит решение через прохождение по ветви, тогда как предлагаемый подход сразу формирует начальное приближение в параметрах \(\mathbf q\). При необходимости перебора нескольких ветвей метод продолжения требует дополнительного выбора ветви, тогда как предлагаемая схема использует структуру аппроксимированного модельного многообразия и конечные формулы для прямого построения приближения.
	
	\item Рассмотрены постановки с ненулевой отлётной скоростью. Показано, что ИРТ-приближение может служить исходной точкой для продолжения по гиперболическому избытку \(\mathbf v_\infty\). Это позволяет связать модельную энергетически оптимальную задачу встречи с более прикладными постановками, в которых отлётная скорость является заданной или оптимизируемой величиной.
	
	\item На примере миссии Земля--Венера продемонстрирована прикладная вычислительная цепочка: построение ИРТ-приближения, переход к решению с ненулевым гиперболическим избытком, дальнейший переход к ПСИ-модели и массовый расчёт по датам старта. В этой цепочке результаты диссертации используются как средство получения и переноса опорных решений для последующего баллистического анализа в более реалистичной модели.
	
	\item Разработанный подход реализован в программном комплексе ILTT Toolbox. Реализация объединяет вычисление конечных формул, построение \(R\)-преобразований, интегрирование расширенной системы, уточнение в параметрах \(\mathbf q\) и сценарии серийного расчёта. Тем самым полученные в работе конечные формулы и преобразования доведены до воспроизводимого вычислительного инструмента.
\end{enumerate}

Таким образом, построен переход от теоретических и численно-аналитических результатов предыдущих глав к прикладному алгоритму баллистического проектирования. Полученный алгоритм позволяет автоматически формировать начальные приближения для энергетически оптимальных межпланетных перелётов, уточнять их в параметрах \(\mathbf q\), использовать их в постановках с ненулевой отлётной скоростью и включать в расчётные цепочки для анализа миссий с электрореактивной двигательной установкой.