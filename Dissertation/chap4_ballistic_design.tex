\chapter{Управляющие параметры начального приближения в задаче баллистического проектирования}

% =========================================================
% Рабочий каркас главы 4.
% Файл содержит только новую структуру разделов и служебные
% комментарии: что писать, какие формулы/рисунки/таблицы брать
% и из каких источников.
%
% ВАЖНО:
% - не писать сюда финальный текст главы на этом этапе;
% - не дублировать компенсацию деформации из главы 3;
% - терминологически держать связку:
%   управляющие параметры, начальный вектор, начальный вектор
%   сопряжённых переменных, начальный вектор расширенной системы;
% - целевая ДВУ обозначается \Omega, управляющий угловой параметр
%   для её задания обозначается \gamma.
% =========================================================

% Основные источники главы:
% - текущие главы 1--3 в GitHub/LaTeX;
% - ВКР: глава 5, особенно §5.1--5.4;
% - презентация НД_диссер_ver12: блок главы 4 и слайды про алгоритм
%   выбора начального приближения, ошибки Марс/Венера, ILTT Toolbox;
% - КорнеевПрезентацияКонференцияКоролёвские2025.pdf:
%   три постановки задачи с отлётной скоростью;
% - Овчинников и др. «Миссия к Венере...»:
%   прикладная цепочка Венера, bang-bang/ПСИ-модель, перенос решения
%   по датам старта через линейное преобразование и \Delta\mathbf v_\infty;
% - biblio/registered.bib: РИД ILTT_Toolbox.

\section{Управляющие параметры начального приближения}
% Задача раздела:
% - ввести главный объект главы:
%   \boldsymbol q=(\lambda,\kappa,\zeta,\chi,j,\gamma);
% - показать, что это не произвольная параметризация, а сборка
%   результатов глав 2 и 3;
% - не использовать старую формулировку «параметры задания начального
%   приближения»; основной термин — «управляющие параметры».
%
% Что брать из главы 2:
% - смысл \lambda как витковой дальности;
% - смысл \kappa как отношения радиусов модельных круговых орбит;
% - связь \lambda,\kappa с выбором точки аппроксимированного
%   модельного многообразия;
% - формулы для \widehat T, \widehat J и \widehat{\mathbf z}_0
%   здесь не переписывать, только ссылаться через \ref.
%
% Что брать из главы 3:
% - \zeta — управляющий параметр R-преобразования II типа;
% - \chi — управляющий угол плоского преобразования для задания
%   долготы перицентра \omega;
% - j — управляющий параметр R-преобразования III типа;
% - \gamma — управляющий угол пространственного преобразования
%   для контроля долготы восходящего узла;
% - отдельно пояснить, что \Omega — целевой орбитальный элемент,
%   а \gamma — параметр матрицы преобразования.
%
% Нужная таблица:
% - 6 строк: \lambda, \kappa, \zeta, \chi, j, \gamma;
% - столбцы: «параметр», «роль», «источник формулы/раздел».
%
% Не брать:
% - старую презентационную шестёрку с \xi;
% - термин «начальные данные»;
% - подробные коэффициенты формул из глав 2--3.

\section{Алгоритм построения начального приближения}
% Задача раздела:
% - дать воспроизводимую схему алгоритма, но не гигантскую таблицу;
% - формулы вставлять через ссылки на главы 2 и 3;
% - раздел должен быть строгим маршрутом, а не презентационным рассказом.
%
% Базовая логика:
%   (\lambda,\kappa)
%   -> \widehat{\mathbf z}_0
%   -> (\zeta,\chi,j,\gamma)
%   -> \mathbf z_0^{init}.
%
% Компактная таблица шагов:
% 1. определить модельные параметры \lambda,\kappa;
% 2. вычислить \widehat T, \widehat J, \widehat{\mathbf z}_0;
% 3. определить \zeta,\chi для задания e,\omega;
% 4. определить j,\gamma для задания i,\Omega;
% 5. применить композицию R-преобразований;
% 6. получить начальный вектор расширенной системы.
%
% Что брать из главы 2:
% - конечные формулы для \widehat T, \widehat J, \widehat{\mathbf z}_0;
% - формулы основной и малой областей, если алгоритм должен указывать
%   выбор области по \lambda.
%
% Что брать из главы 3:
% - формулы II типа для \zeta,\chi;
% - формулы III типа для j,\gamma;
% - раздел композиции R-преобразований;
% - линейную коррекцию витковой дальности только как ссылку на главу 3,
%   без повторного вывода.
%
% Нужный рисунок:
% - компактная блок-схема алгоритма, близкая к презентации, но с
%   текущими обозначениями \boldsymbol q.
%
% Не брать:
% - сценарии использования; они переносятся в финальный раздел 4.8;
% - компенсацию деформации как самостоятельный подраздел.

\section{Пример построения начального приближения для перелёта Земля--Марс}
% Задача раздела:
% - дать один конкретный воспроизводимый численный пример;
% - показать, что алгоритм не только словесный, но и формально
%   воспроизводимый по значениям начального фазового вектора,
%   управляющих параметров и начального вектора сопряжённых переменных.
%
% Что обязательно выписать:
% - дата старта;
% - \mathbf r_0;
% - \mathbf v_0;
% - \widehat{\mathbf z}_0 до R-преобразований;
% - \boldsymbol q=(\lambda,\kappa,\zeta,\chi,j,\gamma);
% - итоговый начальный вектор после композиции R-преобразований;
% - при необходимости \widehat T и \widehat J.
%
% Что брать:
% - ВКР: §5.1 как исходный банк численного примера;
% - презентация: рисунок сравнения начального приближения и
%   оптимизированной траектории для Земля--Марс;
% - текущий код/ноутбук ILTT/главы 4 для точных значений
%   \mathbf r_0, \mathbf v_0, \widehat{\mathbf z}_0 и \boldsymbol q.
%
% Нужная таблица:
% - «величина — значение»;
% - векторные величины записывать столбцами или строками единообразно.
%
% Нужный рисунок:
% - сравнение начального приближения и оптимизированной траектории
%   по типу презентации.

\section{Оценка качества начальных приближений для перелётов Земля--Марс и Земля--Венера}
% Задача раздела:
% - поставить центральную таблицу качества начальных приближений;
% - явно указать, что используется MAE — средняя абсолютная ошибка;
% - пояснить период великих противостояний, потому что аудитория
%   на семинарах этот термин не помнила.
%
% Что брать:
% - презентация НД_диссер_ver12: таблица ошибок для Земля--Марс
%   и Земля--Венера;
% - ВКР: §5.2 как исходный текстовый и численный контекст;
% - при необходимости текущие расчётные таблицы из ноутбуков главы 4.
%
% Что объяснить в комментариях к таблице:
% - таблица содержит MAE по набору дат старта;
% - период великих противостояний — репрезентативный длинный интервал,
%   на котором повторяется наиболее благоприятная геометрия перелёта;
% - для Марса и Венеры периоды различаются;
% - это проверка качества собранного начального приближения, а не
%   повторная верификация формул R-преобразований из главы 3.
%
% Нужная таблица:
% - строки/столбцы сохранить по презентации;
% - в подписи обязательно: «средняя абсолютная ошибка (MAE)».
%
% Не брать:
% - подробные heatmap ошибок R-преобразований из главы 3;
% - компенсацию \lambda как отдельный сюжет.

\section{Сравнение с продолжением по гравитационному параметру}
% Задача раздела:
% - численно сравнить предложенное начальное приближение с классическим
%   подходом продолжения по гравитационному параметру;
% - это НЕ раздел про прямую оптимизацию в сопряжённых переменных;
% - это НЕ раздел про три постановки отлётной скорости.
%
% Метод сравнения:
% - базовый внешний подход: метод В.Г. Петухова / continuation;
% - параметр продолжения можно записать в общем виде:
%   \mu(s)=(1-s)\mu_0+s\mu_1, \quad s\in[0,1];
% - точную форму и обозначения согласовать с используемым численным
%   экспериментом и ссылками на Петухова.
%
% Что брать:
% - литература: Petukhov 2004, 2008, 2012 из biblio/external.bib;
% - ВКР: вводный обзор и глава 5 как старый контекст сравнения;
% - новые расчёты/графики, которые ещё предстоит построить специально
%   для этого раздела.
%
% Какие результаты нужны:
% - число шагов продолжения;
% - число итераций/вызовов функции;
% - итоговая невязка правого конца;
% - устойчивость сходимости;
% - при необходимости время расчёта.
%
% Нужные рисунки/таблицы:
% - график невязки правого конца;
% - график числа итераций или вызовов функции;
% - компактная таблица сравнения по нескольким тестовым задачам.
%
% Не брать:
% - постановки с ненулевой отлётной скоростью из Королёвского доклада;
% - перенос гиперболического избытка из Венеры — это раздел 4.6/4.7.

\section{Постановки задачи с ненулевой отлётной скоростью}
% Задача раздела:
% - ввести переход от задачи встречи с нулевым гиперболическим избытком
%   к задачам с ненулевой отлётной скоростью;
% - целиком использовать логику трёх постановок из Королёвских чтений;
% - не включать линейную интерполяцию как рабочий результат, потому что
%   фактически она не стала устойчивым итоговым методом.
%
% Базовая формула:
%   \mathbf v_0=\mathbf v_C+\mathbf v_\infty.
%
% Три постановки:
% 1. фиксированная отлётная скорость;
% 2. фиксированная величина отлётной скорости;
% 3. свободная отлётная скорость.
%
% Что брать из Королёвского доклада 2025:
% - слайды про модельную задачу эллиптическая--круговая орбита;
% - слайд с условиями трансверсальности на левом конце;
% - слайды про взаимосвязь трёх постановок;
% - условие \mathbf p_v(t_0)=0 для свободной отлётной скорости;
% - экспериментальное наблюдение про p_{ry}(t_0)=0 можно упомянуть
%   осторожно, если оно нужно для интерпретации.
%
% Рабочая схема, которую надо заявить:
% - продолжение от круговой начальной орбиты и нулевого
%   гиперболического избытка к оптимальному \mathbf v_\infty;
% - возможная запись:
%   \mathbf v_\infty(s)=s\mathbf v_\infty^\star, \quad s\in[0,1].
%
% Что добавить из препринта по Венере:
% - линейное преобразование не только начального вектора сопряжённых
%   переменных, но и вектора гиперболического избытка;
% - формулы вокруг R/G-преобразования и \Delta\mathbf v_\infty
%   из раздела «Методика массовых баллистических расчётов»;
% - внимательно сверить соглашение о строках/столбцах и транспонировании
%   с кодом и с формулами препринта.
%
% Не брать:
% - слайд про линейные сплайны как финальный метод;
% - массовый перебор дат старта как основную тему — это раздел 4.7.

\section{Применение алгоритма при проектировании миссии Земля--Венера}
% Задача раздела:
% - показать конкретную прикладную цепочку для миссии к Венере;
% - это не общий обзор массовых расчётов, а пример того, как алгоритм
%   используется от ИР-приближения до ПСИ/bang-bang решения;
% - после получения опорного решения добавить массовый расчёт по датам
%   старта через R-преобразование I типа.
%
% Основная цепочка:
%   ИР-приближение
%   -> продолжение к оптимальному \mathbf v_\infty
%   -> переход к ПСИ-модели и bang-bang управлению
%   -> опорное решение
%   -> перенос опорного решения по датам старта через R-I
%      вместе с \mathbf v_\infty
%   -> пролётные окна для малой тяги.
%
% Что брать из препринта «Миссия к Венере...»:
% - раздел «Баллистический анализ»;
% - подраздел «Межпланетный перелёт»;
% - постановку ПСИ-модели, функцию переключения и активные/пассивные
%   участки из «Методики массовых баллистических расчётов»;
% - таблицу 6 с параметрами опорного перелёта Земля--Венера
%   01.11.2032, 390.8 суток, топливо, \Delta V, \mathbf v_\infty,
%   моторное время;
% - рисунок 5 с опорной траекторией;
% - рисунок 9 с начальным приближением для другой даты, полученным
%   преобразованием из опорного решения;
% - рисунки 10--12: \Delta V, гиперболический избыток и длительность
%   перелёта по датам старта;
% - при необходимости рисунок 13 про выбор точки входа в сферу Хилла,
%   но не делать его центральным.
%
% Что обязательно подчеркнуть:
% - когда цепочка получения опорного решения отработала, массовый расчёт
%   выполняется без повторного продолжения;
% - опорное решение переносится R-преобразованием I типа на весь
%   диапазон дат старта;
% - вместе с начальными сопряжёнными переменными преобразуется
%   и гиперболический избыток;
% - в результате получается классическая картина пролётных окон,
%   но для перелётов с малой тягой.
%
% Не брать:
% - научные задачи Венеры и полезную нагрузку, кроме одной вводной фразы
%   при необходимости;
% - подробный раздел про гало-орбиты, если он не нужен для логики
%   алгоритма главы 4.

\section{Сценарии использования и программная реализация в ILTT Toolbox}
% Задача раздела:
% - финальный прикладной раздел главы;
% - должен быть понятен читателю, который смотрит, что именно сделано
%   и как это применяется;
% - этот раздел вероятно станет финальным содержательным слайдом защиты.
%
% Что брать:
% - biblio/registered.bib: свидетельство о госрегистрации программы
%   для ЭВМ Interplanetary Low Thrust Trajectories Toolbox;
% - презентация НД_диссер_ver12: слайды про ILTT Toolbox и алгоритм
%   автоматического выбора начального приближения;
% - ВКР: глава 5 как старый источник практической линии;
% - препринт Венеры: массовый перенос решения по датам старта как
%   пример практического сценария.
%
% Нужная финальная иллюстрация:
%   дата старта, целевая орбита
%   -> \boldsymbol q
%   -> ИР-приближение
%   -> ненулевой \mathbf v_\infty
%   -> bang-bang траектория
%   -> миссионный анализ.
%
% Нужная таблица сценариев:
% - построение начального приближения -> \mathbf z_0^{init};
% - перенос между датами старта -> новое приближение через R-I;
% - продолжение по \mathbf v_\infty -> решение с ненулевым
%   гиперболическим избытком;
% - переход к ПСИ-модели -> bang-bang траектория;
% - массовый расчёт -> окна старта, \Delta V, расход топлива,
%   длительность перелёта.
%
% Что подчеркнуть:
% - управляющие параметры являются вычислительным интерфейсом между
%   конечными формулами, численным продолжением и прикладным
%   баллистическим проектированием;
% - научный результат и программную реализацию развести, но связать.
%
% Не брать:
% - длинное руководство пользователя;
% - подробности интерфейса программы, если они не работают на защитную
%   логику главы.

\section*{Выводы к главе 4}
% Задача раздела:
% - кратко зафиксировать содержательные результаты главы;
% - без новых формул и без новых рисунков.
%
% Что должно быть в выводах:
% 1. введена шестёрка управляющих параметров
%    \boldsymbol q=(\lambda,\kappa,\zeta,\chi,j,\gamma);
% 2. построен алгоритм получения начального приближения;
% 3. приведён воспроизводимый пример для перелёта Земля--Марс;
% 4. качество начальных приближений для Земля--Марс и Земля--Венера
%    оценено по MAE;
% 5. выполнено сравнение с продолжением по гравитационному параметру;
% 6. рассмотрены постановки с ненулевой отлётной скоростью и схема
%    продолжения к оптимальному гиперболическому избытку;
% 7. показана прикладная цепочка для миссии Земля--Венера;
% 8. метод реализован в ILTT Toolbox и связан с практическими
%    сценариями баллистического проектирования.
